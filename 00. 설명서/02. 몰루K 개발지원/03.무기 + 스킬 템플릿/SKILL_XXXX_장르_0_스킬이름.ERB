; ============================================
; 무기 스킬 템플릿
; XXXX 는 무기 번호 + 1000 로 변경
; 장르는 (SP, BASE, CAPTURE, ESCAPE) 중 선택
; 주석 처리된 함수는 필요하면 활성화 (카운터, 소환 함수 등등)
; ============================================

;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_XXXX_장르_0_EXIST
RETURN 1

@SKILL_XXXX_장르_0_NAME
RESULTS = 

;레벨은 1-5까지
@SKILL_XXXX_장르_0_LEVEL
RETURN 1

;대상 선택
@SKILL_XXXX_장르_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_XXXX_장르_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_XXXX_장르_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_XXXX_장르_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측

;스킬 설명
@SKILL_XXXX_장르_0_EXPLANATION
RESULTS = 

;캐릭터가 소속해 있어도 숍으로부터 구입 할 수 없도록 한다.
@SKILL_XXXX_장르_0_CANT_TELL

; 아이가 배울수 없도록 한다
@SKILL_XXXX_장르_0_CANT_INHERIT

;발동율. 천분율
@SKILL_XXXX_장르_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 1000


; #region == 총력전 ==
; 총력전 스킬 코스트
@총력전_스킬_XXXX_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
RETURN 2

; 기본 쿨(2) 이랑 다르게 하고 싶으면 활성화
; 가변은 스킬 
; @총력전_스킬_XXXX_쿨다운(가변)
; #DIM 가변
; RETURN 2

; 총려전 스킬 설명
@총력전_스킬_XXXX_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 

; 총력전 시작시 발동, 필요하면 활성화
; @총력전_스킬_XXXX_초기(시전자)
; #DIM 시전자

; 총력전 스킬 발동
@총력전_스킬_XXXX_발동(시전자)
#DIM 시전자


; #region == 소환물 ==
; XXXX = 스킬 번호, X = 추가 번호
; 추가 번호는 총력전_스킬_가변 으로 변경 + 확인 (나중에 변경 될수 있음, 햇갈릴거 같아서;;;)
; @총력전_소환물_수 참조

; 소환물 (자동) 스킬 있는지 확인
; @총력전_소환_스킬_XXXX_X_EXIST
; RETURN 1

; 소환물 (자동) 스킬 시전, 토라마루 참조
; 캐릭터 스킬 -> 소환수 스킬 차례로 발동
; @총력전_소환_스킬_XXXX_X_발동(소환물)
; #DIM 소환물

; 소환물 해제 전 호출되는 함수
; @총력전_소환_XXXX_X_해제(소환물)
; #DIM 소환물


; #region == 카운터 ==
; XXXX = 스킬 번호, X = 추가 번호
; 예. CALL 총력전_카운터_추가(시전자, 시전자, 총력전_방식_공격, 총력전_방식_턴, 3, 9000, 4)
; = 총력전_스킬_9000_4_카운터_증가

; 카운터 증가시 자동으로 수행하는 *대신* 호출되는 함수
; @총력전_스킬_XXXX_X_카운터_증가(캐릭터, 대상, 카운터번호, 증가양)
; #DIM 캐릭터
; #DIM 대상
; #DIM 카운터번호
; #DIM 증가양

; 카운터 종료시 자동으로 수행하는 *대신* 호출되는 함수
; @총력전_스킬_XXXX_X_카운터_종료(대상, 카운터번호)
; #DIM 대상
; #DIM 카운터번호
