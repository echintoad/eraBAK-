;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1104_BASE_0_EXIST
RETURN 1

@SKILL_1104_BASE_0_NAME
RESULTS = 지식의 방패

;레벨은 1-5까지
@SKILL_1104_BASE_0_LEVEL
RETURN 3

;대상 선택
@SKILL_1104_BASE_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상

;발동 판정
@SKILL_1104_BASE_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1104_BASE_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1104_BASE_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "여러분~ 독서를 더 많이 해주세요!"
TIMES BATTLE_DEF:발동측, 1.20
TIMES BATTLE_INT:발동측, 1.50


@SKILL_1104_BASE_0_EXPLANATION
RESULTS = 자신 부대의 방위와 지략을 상승시킨다.

@SKILL_1104_BASE_0_CANT_TELL

; 아이가 배울수 없도록 한다
@SKILL_1104_BASE_0_CANT_INHERIT

@SKILL_1104_BASE_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 300

; #region == 총력전 ==
@총력전_스킬_1104_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 3

; 테스트 동안
; @총력전_스킬_1104_쿨다운(가변)
; #DIM 가변
; RETURN 1

; alt.opt.1 소환 -> 소환 위치 주변으로 방어력 버프
; alt.opt.2 엄폐물 스킬: 주변으로(1범위) 방어력 버프 <- 이 방식이면 엄폐물 사라지면 방어력 버프도 사라짐
@총력전_스킬_1104_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 최대 세명의 아군에게 3턴간 방어력 26％ 증가 추가로 최대 체력 38％의 엄폐물 생성

@총력전_스킬_1104_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 버프대상, 3
#DIM CONST 방어력증가량 = 26
; 엄폐물
#DIM DYNAMIC 엄폐물위치
#DIM DYNAMIC 소비방식, 2
#DIM DYNAMIC 소비양, 2
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「여러분~ 독서를 더 많이 해주세요!」
sTmp:1 = 「저와 함께 책을 읽죠!」
sTmp:2 = 「이것 또한, 책의 힘이에요!」
CALL SKILL_1104_BASE_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 60, RESULTS, sTmp)
; 목표 처리
sTmp = 버프 대상 선택
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
CALL 총력전_시퀀스_목표선택(버프대상, 총력전_선택_아군)
; 목표 없으면 스킬 해재
IF 버프대상 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 버프
VARSIZE 버프대상
FOR iTmp, 0, RESULT
	SIF 버프대상:iTmp < 0
		BREAK
	CALL 총력전_상태효과_추가_텍스트(버프대상:iTmp, 시전자, 상효_종류_방어력백분율, 방어력증가량, 1, 총력전_방식_지속, 총력전_방식_턴, 3, 1104)
NEXT
; 소환 위치 처리
sTmp = 엄폐물 소환 위치 선택
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
CALL 총력전_시퀀스_목표선택(엄폐물위치, 총력전_선택_아군지역_엄폐물)
; 엄폐물 번호가 -1 이면 스킬 해재
IF 엄폐물위치 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 엄폐물 중복 확인
iTmp = 총력전_엄폐물_검색_스킬번호(1104)
; 있으면 엄폐물 제거
IF iTmp >= 0
	; CALL 총력전_엄폐물_제거_텍스트(iTmp) ; 옵션 2
	CALL 총력전_해제_엄폐물(iTmp)
ENDIF
; 총력전 번호 -> 위치로 변경
엄폐물위치 = 총력전_위치:엄폐물위치
; 엄폐물 소환, 체력 0 또는 3 턴 이후 소멸
소비방식:0 = 총력전_방식_피격양
소비양:0 = 총력전_최대체력_최초:시전자 * 38
소비양:0 = DIVIDE_ROUND(소비양:0, 100)
소비방식:1 = 총력전_방식_턴
소비양:1 = 3
CALL 총력전_엄폐물_추가(엄폐물위치, 시전자, 소비방식, 소비양, 1104)


; 엄폐물체력 = (MAXBASE:(총력전_파티_캐릭터:LOCAL):체력 * 38 / 100)
; 방어력증가량 = 26
; 대상수 = 3
; CALL 버프처리(1,,,,,,,,,,방어력증가량,,대상수,1104)
; CALL 엄폐물처리(엄폐물체력)

; @총력전_스킬_1104_버프지속
; RETURN 3