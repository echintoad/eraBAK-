;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1601_SP_0_EXIST
RETURN 1

@SKILL_1601_SP_0_NAME
RESULTS = 아플 확률이 높아

;레벨은 1-5까지
@SKILL_1601_SP_0_LEVEL
RETURN 3

;대상 선택
@SKILL_1601_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상

RETURN 1

;발동 판정
@SKILL_1601_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1601_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1601_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "적 위치 확인. 발포 준비 완료."
LOCAL:1 = 5
FOR LOCAL, 0, LOCAL:1
	CALL DECREASE_SOLDIER(대상세력, 대상부대, RAND(ABL:발동자:지략*10 , ABL:발동자:지략*15), 1)
NEXT

@SKILL_1601_SP_0_EXPLANATION
RESULTS = 적 부대에 연속 데미지.

@SKILL_1601_SP_0_CANT_TELL


@SKILL_1601_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 250
 
; #region == 총력전 ==
 @총력전_스킬_1601_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
 RETURN 4
 
; alt.opt.1 무작위 적 범위 공격 x5
 @총력전_스킬_1601_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
; 1200%
RESULTS = 모든 적에게 공격력 240％x5 대미지 
 
 @총력전_스킬_1601_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 목표, 총력전_캐릭터수
#DIM DYNAMIC 대미지
#DIM DYNAMIC iTmp, 2
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「적 위치 확인… 발포 준비 완료.」
sTmp:1 = 「지금 장전하는건… 분노.」
sTmp:2 = 「그럭저럭… 큰일이 날지도?」
CALL SKILL_1601_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 47, RESULTS, sTmp)
; 목표 처리
CALL 총력전_목표_전체(목표, 1)
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 240
대미지 = DIVIDE_ROUND(대미지, 100)
FOR iTmp, 0, 총력전_캐릭터수
	SIF 목표:iTmp < 0
		BREAK
	FOR iTmp:1, 0, 5
		CALL 총력전_처리_피격(대미지, 목표:iTmp, 시전자)
	NEXT
NEXT


;  #DIM 대미지
;  대미지 = (총력전_공격력:LOCAL * 400 / 100) * (100 - 총력전_보스_방어력) / 100
;  FOR COUNT, 0, 3
;  CALL 공격판정처리(대미지,LOCAL)
;  NEXT