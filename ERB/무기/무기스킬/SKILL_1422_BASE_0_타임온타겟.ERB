;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1422_BASE_0_EXIST
RETURN 1

@SKILL_1422_BASE_0_NAME
RESULTS = Time On Target

;레벨은 1-5까지
@SKILL_1422_BASE_0_LEVEL
RETURN 5

;대상 선택
@SKILL_1422_BASE_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상

RETURN 1

;발동 판정
@SKILL_1422_BASE_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1422_BASE_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1422_BASE_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "멋진 티 타임에 음악이 빠질 순 없겠지요."
TIMES BATTLE_DEF:대상측, 0.85
LOCAL:1 = RAND(4, 12)
FOR LOCAL, 0, LOCAL:1
	CALL DECREASE_SOLDIER(대상세력, 대상부대, RAND(ABL:발동자:지략, ABL:발동자:지략*5), 1)
NEXT

@SKILL_1422_BASE_0_EXPLANATION
RESULTS = 적 부대의 방어력을 감소시키고 연속 데미지를 준다.

@SKILL_1422_BASE_0_CANT_TELL


@SKILL_1422_BASE_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 450

; #region == 총력전 ==
@총력전_스킬_1422_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 3

; 버프+디버프 순서 수정, 개발노트 "스킬 디자인" 참조
; alt.opt.1 트리니티(태그) 학생 추가 버프?
@총력전_스킬_1422_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 모든 적에게 공격력 772％ 대미지후 2턴간 적의 방어력 40％ 감소 아군 전원의 치명대미지 20％ 증가 


@총력전_스킬_1422_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 목표, 총력전_캐릭터수
; #DIM DYNAMIC 디버프목표, 총력전_캐릭터수
#DIM DYNAMIC 대미지
#DIM CONST 방깎수치 = -40
#DIM CONST 치명타대미지증가량 = 20
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「멋진 티 타임에 음악이 빠질 순 없겠지요.」
sTmp:1 = 「예포는 약식으로. 다만, 경의를 잊지 마세요.」
sTmp:2 = 「포수, 지원을.」
CALL SKILL_1422_BASE_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 50, RESULTS, sTmp)
; 목표 처리
CALL 총력전_목표_전체(목표, 1)
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 772
대미지 = DIVIDE_ROUND(대미지, 100)
FOR iTmp, 0, 총력전_캐릭터수
	SIF 목표:iTmp < 0
		BREAK
	CALL 총력전_처리_피격(대미지, 목표:iTmp, 시전자)
	; 방어 감소
	CALL 총력전_상태효과_추가_텍스트(목표:iTmp, 시전자, 상효_종류_방어력백분율, 방깎수치, 0, 총력전_방식_지속, 총력전_방식_턴, 2, 1422)
NEXT
; 버프 목표 처리
CALL 총력전_목표_전체(목표, 0)
; 버프
FOR iTmp, 0, 총력전_캐릭터수
	SIF 목표:iTmp < 0
		BREAK
	CALL 총력전_상태효과_추가_텍스트(목표:iTmp, 시전자, 상효_종류_치명타대미지, 치명타대미지증가량, 1, 총력전_방식_지속, 총력전_방식_턴, 2, 1422)
NEXT


; 치명타대미지증가량 = 20
; 방깎수치 = 40
; 대미지 = (총력전_공격력:LOCAL * 772 / 100) * (100 - 총력전_보스_방어력) / 100
; CALL 버프처리(1,,,,,,,치명타대미지증가량,,,,,5,1422)
; CALL 디버프부여처리("방깎",1422,2,LOCAL,방깎수치)
; CALL 공격판정처리(대미지,LOCAL)

; @총력전_스킬_1422_버프지속
; RETURN 2