;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1509_SP_0_EXIST
RETURN 1

@SKILL_1509_SP_0_NAME
RESULTS = 작열의 세레나데

;레벨은 1-5까지
@SKILL_1509_SP_0_LEVEL
RETURN 3

;대상 선택
@SKILL_1509_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
;부대 대상
RETURN 1

;발동 판정
@SKILL_1509_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;효과를 여기에 기술
@SKILL_1509_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
CALL DECREASE_SOLDIER(대상세력, 대상부대, RAND(ABL:발동자:무투*5, ABL:발동자:무투*30), 1)
; SELECTCASE RAND:6
;     CASE 0
;         PRINTFORML 「짠! 불꽃놀이 시작해 볼까?」
;     CASE 1
;         PRINTFORML 「두근두근하는 시간이 다가왔어~!」
;     CASE 2
;         PRINTFORML 「자, 간다~!」
;     CASE 3
;         PRINTFORML 「꺄하하! 팝콘 같아!」
;     CASE 4
;         PRINTFORML 「받아라~! 빵! 아하하, 재밌다~!」
;     CASE 5
;         PRINTFORML 「쾅쾅 터져버려~ 폭.발.해.라♥」
; ENDSELECT

@SKILL_1509_SP_0_EXPLANATION
RESULTS = 폭탄을 던져 피해를 준다.




@SKILL_1509_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 500

; #region == 총력전 ==
@총력전_스킬_1509_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 4

@총력전_스킬_1509_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
; 1149 = 383
RESULTS = 지정된 적과 2범위 안 적에게 공격력 383％x3 대미지

@총력전_스킬_1509_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 주목표 ; 목표
#DIM DYNAMIC 범위목표, 총력전_캐릭터수 ; 목표 + 주변 적
#DIM DYNAMIC 대미지
#DIM DYNAMIC iTmp, 2
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「짠! 불꽃놀이 시작해 볼까?」
sTmp:1 = 「받아라~! 빵! 아하하, 재밌다~!」
sTmp:2 = 「쾅쾅 터져버려~ 폭.발.해.라♥」
CALL SKILL_1509_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 10, RESULTS, sTmp)
; 목표 처리
CALL 총력전_시퀀스_목표선택(주목표, 총력전_선택_적군)
; 목표 없으면 스킬 해재
IF 주목표 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 범위로 변경, 적팀 1 이상이 되면 수정 필요
CALL 총력전_목표_범위(범위목표, 주목표, 1, 2)
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 383
대미지 = DIVIDE_ROUND(대미지, 100)
FOR iTmp:1, 0, 3
	; 범위로 변경
	FOR iTmp, 0, 총력전_캐릭터수
		IF 범위목표:iTmp < 0
			BREAK
		ENDIF
		CALL 총력전_처리_피격(대미지, 범위목표:iTmp, 시전자)
	NEXT
NEXT