;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1407_SP_0_EXIST
RETURN 1

@SKILL_1407_SP_0_NAME
RESULTS = 기록의 생명은 속도랍니다

;레벨은 1-5까지
@SKILL_1407_SP_0_LEVEL
RETURN 4

;대상 선택
@SKILL_1407_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_1407_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대

RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1407_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1407_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "과연⋯ 거기가 약점이군요."
BATTLE_RATE_ATC:대상측 += 15

@SKILL_1407_SP_0_EXPLANATION
RESULTS = 적 부대의 받는 피해를 증가시킨다.


@SKILL_1407_SP_0_CANT_TELL


@SKILL_1407_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 450

; #region == 총력전 ==
@총력전_스킬_1407_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 3

@총력전_스킬_1407_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 4턴간 적의 방어력 40％ 감소 와 도발 적용
; RESULTS = 4턴간 적의 방어력 40％ 감소, 집중공격 적용

@총력전_스킬_1407_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 목표
#DIM CONST 방깎수치 = -40
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「과연… 거기가 약점이군요.」
sTmp:1 = 「헤에… 이건 흥미롭네요.」
sTmp:2 = 「숨으려 해봤자, 저의 기록에서는 벗어날 수 없답니다!」
CALL SKILL_1407_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 29, RESULTS, sTmp)
; 목표 처리
CALL 총력전_시퀀스_목표선택(목표, 총력전_선택_적군)
; 목표 없으면 스킬 해재
IF 목표 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 방어 감소
CALL 총력전_상태효과_추가_텍스트(목표, 시전자, 상효_종류_방어력백분율, 방깎수치, 0, 총력전_방식_지속, 총력전_방식_턴, 4, 1407, 0)
CALL 총력전_상태효과_추가(목표, 시전자, 상효_종류_도발, 1, 0, 총력전_방식_지속, 총력전_방식_턴, 4, 1407, 1)
; 텍스트 추가
; sTmp = %조사처리(총력전_이름:목표, "의")% 방어력 ({방깎수치}％) 감소!
; CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
; ; UI 업데이트
; CALL 총력전_UI_Main(0)
; WAITANYKEY