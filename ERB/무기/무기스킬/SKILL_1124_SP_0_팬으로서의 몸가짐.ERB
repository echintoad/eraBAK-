;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1124_SP_0_EXIST
RETURN 1

@SKILL_1124_SP_0_NAME
RESULTS = 팬으로서의 몸가짐

;레벨은 1-5까지
@SKILL_1124_SP_0_LEVEL
RETURN 4

;대상 선택
@SKILL_1124_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_1124_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1124_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1124_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측

IF SKILL_1124_SP_0_CHECK_COMMANDER(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
	; PRINTFORML "이... 이건? 치세 쨩!!!"
    ; PRINTFORML 치세와 함께한 카호의 일격!
	LOCAL:1 = RAND(1, 3)
FOR LOCAL, 0, LOCAL:1
	CALL DECREASE_SOLDIER(대상세력, 대상부대, ABL:발동자:지략*15, 1)
NEXT

ELSE
	CALL DECREASE_SOLDIER(대상세력, 대상부대, ABL:발동자:지략*5, 1)
ENDIF

@SKILL_1124_SP_0_EXPLANATION
RESULTS = 적 부대에 데미지를 준다. 치세와 함께라면 큰 데미지를 준다.

@SKILL_1124_SP_0_CANT_TELL

; 아이가 배울수 없도록 한다
@SKILL_1124_SP_0_CANT_INHERIT


@SKILL_1124_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 1000

@SKILL_1124_SP_0_CHECK_COMMANDER(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#FUNCTION
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
FOR LOCAL, 0, BATTLE_COMMANDER_NUM:발동측
	IF BATTLE_COMMANDER:발동측:LOCAL == NAME_TO_CHARA("치세")
		RETURNF 1
	ENDIF
NEXT
RETURNF 0

; #region == 총력전 ==
@총력전_스킬_1124_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 5

; 총력전 시작시 시전
@총력전_스킬_1124_초기(시전자)
#DIM 시전자
; 『명견지수』, 5턴 마다 2턴 버프
CALL 총력전_카운터_추가(시전자, 시전자, 총력전_방식_턴, 총력전_방식_지속, 1, 1124)

; 추가 공격도 추가 하고 싶으면 공속 증가 또는 사쿠라코 세례의 증표 참조
; 나중에 치세 관련 해서 뭔가 추가 해야될듯
@총력전_스킬_1124_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
; 1500%
RESULTS = 적에게 공격력 350％x2 + 800％ 대미지. 5턴 마다 2턴동안 공격력 25％ 증가.

@총력전_스킬_1124_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 목표
#DIM DYNAMIC 대미지
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「앗?! 하늘에서… 이건?!」
sTmp:1 = 「떨어지게 놔둘 순, 없어요!」
sTmp:2 = 「모두 회수하겠어요!」
CALL SKILL_1124_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 76, RESULTS, sTmp)
; 목표 처리
CALL 총력전_시퀀스_목표선택(목표, 총력전_선택_적군)
; 목표 없으면 스킬 해재
IF 목표 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 350
대미지 = DIVIDE_ROUND(대미지, 100)
FOR iTmp, 0, 2
	CALL 총력전_처리_피격(대미지, 목표, 시전자)
NEXT
; 막타 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 800
대미지 = DIVIDE_ROUND(대미지, 100)
CALL 총력전_처리_피격(대미지, 목표, 시전자)


; #region == 카운터 ==
@총력전_스킬_1124_0_카운터_증가(캐릭터, 대상, 카운터번호, 증가양)
#DIM 캐릭터
#DIM 대상
#DIM 카운터번호
#DIM 증가양

총력전_카운터_증가양:캐릭터:카운터번호 += 증가양
IF 총력전_카운터_증가양:캐릭터:카운터번호 >= 5
	총력전_카운터_증가양:캐릭터:카운터번호 = 0
	CALL 총력전_상태효과_추가_텍스트(캐릭터, 캐릭터, 상효_종류_공격력백분율, 25, 1, 총력전_방식_지속, 총력전_방식_턴, 2, 1124)
ENDIF