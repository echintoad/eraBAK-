;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1411_SP_0_EXIST
RETURN 1

@SKILL_1411_SP_0_NAME
RESULTS = 협상은 없습니다!

;레벨은 1-5까지
@SKILL_1411_SP_0_LEVEL
RETURN 3

;대상 선택
@SKILL_1411_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_1411_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
 
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1411_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1411_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "경고합니다! 투항하지 않으면...우왓...!"
BATTLE_RATE_GRD:발동측 += 5

@SKILL_1411_SP_0_EXPLANATION
RESULTS = 자부대의 받은 피해를 경감시킨다.


@SKILL_1411_SP_0_CANT_TELL


@SKILL_1411_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 300

; #region == 총력전 ==
@총력전_스킬_1411_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 3

@총력전_스킬_1411_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
; 1범위?
RESULTS = 적에게 공격력 540％ 대미지 추가로 2턴간 확정 명중률 감소 (-30％)
; RESULTS = 적에게 공격력 540％ 대미지 추가로 2턴간 모든 아군의 회피율 30％ 증가

@총력전_스킬_1411_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 목표
#DIM DYNAMIC 대미지
#DIM CONST 명중감소 = -30
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 4

; 발동 텍스트
sTmp:0 = 「당신은 묵비권을 행사할 수 있고… 우, 우앗?」
sTmp:1 = 「경고합니다! 투항하지 않으면… 우왓!」
sTmp:2 = 「어쩌지, 어쩌지? …에잇!」
sTmp:3 = 「고, 공권력을 투입합니다!」
CALL SKILL_1411_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 105, RESULTS, sTmp)
; 목표 처리
CALL 총력전_시퀀스_목표선택(목표, 총력전_선택_적군)
; 목표 없으면 스킬 해재
IF 목표 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 540
대미지 = DIVIDE_ROUND(대미지, 100)
CALL 총력전_처리_피격(대미지, 목표, 시전자)
; 확정 명중 감소
CALL 총력전_상태효과_추가_텍스트(목표, 시전자, 상효_종류_명중률백분율, 명중감소, 0, 총력전_방식_지속, 총력전_방식_턴, 2, 1411)




; 회피율증가량 = 30
; 대미지 = (총력전_공격력:LOCAL * 540 / 100) * (100 - 총력전_보스_방어력) / 100
; CALL 공격판정처리(대미지,LOCAL)
; CALL 버프처리(1,,,,,,,,,회피율증가량,,,5,1411)

; @총력전_스킬_1411_버프지속
; RETURN 2