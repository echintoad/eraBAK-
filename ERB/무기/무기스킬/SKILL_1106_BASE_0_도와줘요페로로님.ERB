;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1106_BASE_0_EXIST
RETURN 1

@SKILL_1106_BASE_0_NAME
RESULTS = 도와줘요, 페로로 님!

;레벨은 1-5까지
@SKILL_1106_BASE_0_LEVEL
RETURN 1

;대상 선택
@SKILL_1106_BASE_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상

RETURN 1

;발동 판정
@SKILL_1106_BASE_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1106_BASE_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1106_BASE_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;PRINTFORML "페로로 님이 나설 차례에요!"
PRINTFORML 춤추는 페로로 인형을 본 적들은 무언가에 홀린 듯 하다! 
TIMES BATTLE_ATK:대상측, 0.8
TIMES BATTLE_INT:대상측, 0.8

@SKILL_1106_BASE_0_EXPLANATION
RESULTS = 적 부대의 무투와 지략을 감소시킨다.

@SKILL_1106_BASE_0_CANT_TELL


@SKILL_1106_BASE_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 250

; #region == 총력전 ==
@총력전_스킬_1106_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 5

@총력전_스킬_1106_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 모든 적에게 공격력 386％의 대미지와 3턴간 최대 체력 160％의 도발『페로로 인형』소환

@총력전_스킬_1106_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 목표, 총력전_캐릭터수
#DIM DYNAMIC 소환물번호
; #DIM DYNAMIC 소환물체력
#DIM DYNAMIC 대미지
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「에엣, 어라, 어디갔지?」
sTmp:1 = 「페로로 님이 나설 차례에요!」
sTmp:2 = 「페로로 님! 부탁해요!」
CALL SKILL_1106_BASE_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 53, RESULTS, sTmp)
; 소환 위치 처리
sTmp = 소환 위치 선택
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
CALL 총력전_시퀀스_목표선택(소환물번호, 총력전_선택_빈아군지역)
; 소환물 번호가 -1 이면 스킬 해재
IF 소환물번호 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 페로로 이미 소환 돼있는지 확인, 시전자가 달라도
; 있으면 소환되있는거 해제
iTmp = 총력전_소환물_번호(총력전_스킬_번호:시전자) 
IF iTmp >= 0
	CALL 총력전_소환_1106_해제(iTmp)
	CALL 총력전_해제_캐릭터(iTmp)
ENDIF
; 소환
CALL 총력전_소환물_소환(소환물번호, 시전자)
; 추가 스탯 설정
총력전_상태:소환물번호 = 총력전_상태_소한_비공격
총력전_이름:소환물번호 = 페로로 인형
iTmp = 총력전_최대체력_최초:시전자 * 160
iTmp = DIVIDE_ROUND(iTmp, 100)
총력전_최대체력_최초:소환물번호 = iTmp
CALL 총력전_초기화_스탯(소환물번호, 1)
; 목표 처리
CALL 총력전_목표_전체(목표, 1)
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 386
대미지 = DIVIDE_ROUND(대미지, 100)
FOR iTmp, 0, 총력전_캐릭터수
	SIF 목표:iTmp < 0
		BREAK
	CALL 총력전_처리_피격(대미지, 목표:iTmp, 시전자)
NEXT
; 페로로 도발 적용
CALL 총력전_상태효과_추가_텍스트(소환물번호, 소환물번호, 상효_종류_도발, 1, 2, 총력전_방식_지속, 총력전_방식_턴, 3, 1106)
; 카운터(지속시간) 설정
CALL 총력전_카운터_추가(소환물번호, 소환물번호, 총력전_방식_지속, 총력전_방식_턴, 3, 1106)



; 소환물체력 = (MAXBASE:(총력전_파티_캐릭터:LOCAL):체력 * 160 / 100)
; 대미지 = (총력전_공격력:LOCAL * 386 / 100)
; CALL 공격판정처리(대미지,LOCAL)
; CALL 소환물처리(소환물체력,"페로로 인형",1106,3,1)

; @총력전_스킬_1106_버프지속
; RETURN 3

; #region == 소환물 ==
@총력전_소환_1106_해제(소환물)
#DIM 소환물

#DIMS DYNAMIC sTmp

sTmp = 『페로로 인형』소환 해제
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)

; #region == 카운터 ==
@총력전_스킬_1106_0_카운터_종료(대상, 카운터번호)
#DIM 대상
#DIM 카운터번호

CALL 총력전_소환_1106_해제(대상)
CALL 총력전_해제_캐릭터(대상)
