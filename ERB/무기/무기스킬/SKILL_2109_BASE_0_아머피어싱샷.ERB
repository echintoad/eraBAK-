;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_2109_BASE_0_EXIST
RETURN 1

@SKILL_2109_BASE_0_NAME
RESULTS = 아머피어싱샷

;레벨은 1-5까지
@SKILL_2109_BASE_0_LEVEL
RETURN 4

;대상 선택
@SKILL_2109_BASE_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_2109_BASE_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_2109_BASE_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_2109_BASE_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "타겟 발견."
CALL DECREASE_SOLDIER(대상세력, 대상부대, MIN(RAND(ABL:발동자:무투*20 ,ABL:발동자:무투*30) , RAND(ABL:발동자:지략*20 ,ABL:발동자:지략*30)), 1)
TIMES BATTLE_DEF:대상측, 0.85



@SKILL_2109_BASE_0_EXPLANATION
RESULTS = 적 부대에 데미지를 주고 방어력을 낮춘다.

@SKILL_2109_BASE_0_CANT_TELL


@SKILL_2109_BASE_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 700

; #region == 총력전 ==
@총력전_스킬_2109_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 5

; alt.opt.1 자기 버프수 당 10% 치명 대미지 증가 후 확정 치면 대미지
; alt.opt.2 적 처치시 무작위 적 추가 대미지
@총력전_스킬_2109_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 1턴간 자신의 치명타 대미지 30％ 증가 추가로 적에게 공격력 1091％ 확정 치명 대미지

@총력전_스킬_2109_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 목표
#DIM DYNAMIC 대미지
#DIM CONST 치명대미지증가량 = 30
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「타겟 발견.」
sTmp:1 = 「놓치지 않습니다.」
sTmp:2 = 「꿰뚫려라!」
CALL SKILL_2109_BASE_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 69, RESULTS, sTmp)
; 목표 처리
CALL 총력전_시퀀스_목표선택(목표, 총력전_선택_적군)
; 목표 없으면 스킬 해재
IF 목표 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 버프
CALL 총력전_상태효과_추가_텍스트(시전자, 시전자, 상효_종류_치명타대미지, 치명대미지증가량, 1, 총력전_방식_스킬, 총력전_방식_스킬, 1, 2109)
; CALL 총력전_상태효과_스탯처리(시전자, 총력전_방식_스킬)
; ; 텍스트 추가
; sTmp = %조사처리(총력전_이름:시전자, "의")% 치명타 대미지 ({치명대미지증가량}％) 증가!
; CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
; ; UI 업데이트
; CALL 총력전_UI_Main(0)
; WAITANYKEY
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 1091
대미지 = DIVIDE_ROUND(대미지, 100)
CALL 총력전_처리_피격(대미지, 목표, 시전자, 1, 2)
; 적 사망시 추가 공격, 아직 테스트 안해봄
; IF 총력전_체력:목표 <= 0
; 	; 무작위 목표 처리
; 	CALL 총력전_목표_무작위(1)
; 	목표 = RESULT
; 	IF 목표 < 0
; 		RETURN
; 	ENDIF
; 	; 대미지, 추가 계산 등 원하면 추가
; 	CALL 총력전_처리_피격(대미지, 목표, 시전자)
; ENDIF
