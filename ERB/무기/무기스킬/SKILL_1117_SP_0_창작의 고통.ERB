;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1117_SP_0_EXIST
RETURN 1

@SKILL_1117_SP_0_NAME
RESULTS = 창작의 고통

;레벨은 1-5까지
@SKILL_1117_SP_0_LEVEL
RETURN 3

;대상 선택
@SKILL_1117_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_1117_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측

RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1117_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1117_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "아이스크림 쏘게 됐잖아! 너네들 때문이야!"
CALL DECREASE_SOLDIER(대상세력, 대상부대, RAND(1000, 1500), 1)

@SKILL_1117_SP_0_EXPLANATION
RESULTS = 적에게 데미지를 준다.

@SKILL_1117_SP_0_CANT_TELL


@SKILL_1117_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 400

; #region == 총력전 ==
@총력전_스킬_1117_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 3

@총력전_스킬_1117_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 1번위 적에게 공격력 180％x3 대미지, 미도리가 함께 있을 경우 추가로 3턴간 공격력 93％ 지속 대미지 부여

@총력전_스킬_1117_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 주목표
#DIM DYNAMIC 범위목표, 총력전_캐릭터수
#DIM DYNAMIC 대미지
#DIM DYNAMIC 지속대미지
#DIM DYNAMIC 미도리찾기
#DIM DYNAMIC iTmp, 2
#DIMS DYNAMIC sTmp

; 발동 텍스트
CALL SKILL_1117_SP_0_NAME
sTmp = %총력전_이름:시전자% 스킬 시전:『%RESULTS%』
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
iTmp = 총력전_ID:시전자
IF iTmp >= 0 && NO:iTmp == 33
	; 모모이일 경우
	SELECTCASE RAND:3
		CASE 0
			sTmp = 「내가 이겼!…」
			CALL 총력전_UI_텍스트_추가(sTmp)
			sTmp = 「엣?! 졌잖아?! 진짜아아아!」
		CASE 1
			sTmp = 「아이스크림 쏘게 됐잖아!」
			CALL 총력전_UI_텍스트_추가(sTmp)
			sTmp = 「너네들 때문이야!」
		CASEELSE
			sTmp = 「내 분노의 탄환을 받아랏!」
	ENDSELECT
	CALL 총력전_UI_텍스트_추가(sTmp)
ENDIF
; 목표 처리
CALL 총력전_시퀀스_목표선택(주목표, 총력전_선택_적군)
; 범위로 변경, 적팀 1 이상이 되면 수정 필요
CALL 총력전_목표_범위(범위목표, 주목표, 1, 1)
; 목표 없으면 스킬 해재
IF 주목표 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 미도리 찾기
FOR iTmp, 0, 총력전_캐릭터수
	iTmp:1 = 총력전_ID:iTmp
	; + 팀 확인?
	IF iTmp:1 >= 0 && NO:(iTmp:1) == 34
		미도리찾기 = 1
		BREAK
	ENDIF
NEXT
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 180
대미지 = DIVIDE_ROUND(대미지, 100)
지속대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 93
지속대미지 = DIVIDE_ROUND(지속대미지, 100)
FOR iTmp, 0, 총력전_캐릭터수
	SIF 범위목표:iTmp < 0
		BREAK
	FOR iTmp:1, 0, 3
		CALL 총력전_처리_피격(대미지, 범위목표:iTmp, 시전자)
	NEXT
	IF 미도리찾기 == 1
		CALL 총력전_상태효과_추가_텍스트(범위목표:iTmp, 시전자, 상효_종류_대미지, 지속대미지, 0, 총력전_방식_턴, 총력전_방식_턴, 3, 1117)
		; 텍스트 추가
		; sTmp = %조사처리(총력전_이름:(범위목표:iTmp), "에")% 지속 대미지 ({지속대미지})!
		; CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
		; ; UI 업데이트
		; CALL 총력전_UI_Main(0)
		; WAITANYKEY
	ENDIF
NEXT


; #DIM 대미지
; #DIM 미도리찾기
; 미도리찾기 = 0

; FOR (LOCAL:1), 0, 총력전_파티_캐릭터수
;     SIF 총력전_파티_캐릭터:(LOCAL:1) == 34
;     미도리찾기 = 1
; NEXT

; 모모이화상대미지 = (총력전_공격력:LOCAL * 93 / 100)
; 대미지 = (총력전_공격력:LOCAL * 541 / 100) * (100 - 총력전_보스_방어력) / 100
; CALL 공격판정처리(대미지,LOCAL)

; SIF 미도리찾기 == 1
; CALL 디버프부여처리("화상",1117,2,LOCAL,모모이화상대미지)
