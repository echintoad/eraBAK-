;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1404_BASE_0_EXIST
RETURN 1

@SKILL_1404_BASE_0_NAME
RESULTS = 공안국장의 수완

;레벨은 1-5까지
@SKILL_1404_BASE_0_LEVEL
RETURN 4

;대상 선택
@SKILL_1404_BASE_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_1404_BASE_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1404_BASE_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1404_BASE_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "자...심문을 시작하지..."
UNIT_TIRED_COUNT:대상세력:대상부대 += RAND(2, 6)
LOCAL:1 = RAND(3, 5)
FOR LOCAL, 0, LOCAL:1
	CALL DECREASE_SOLDIER(대상세력, 대상부대, ABL:발동자:방위*5, 1)
NEXT

@SKILL_1404_BASE_0_EXPLANATION
RESULTS = 적부대의 피로도를 증가시키고 데미지를 준다.


@SKILL_1404_BASE_0_CANT_TELL

; 아이가 배울수 없도록 한다
@SKILL_1404_BASE_0_CANT_INHERIT


@SKILL_1404_BASE_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 700

; #region == 총력전 ==
@총력전_스킬_1404_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 3

@총력전_스킬_1404_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 적에게 공격력 100％ 대미지 추가로 확정 2턴간 아군이 입힌 대미지의 50％ 축적 축적대미지는 공격력 2000％ 까지 적용
; RESULTS = 적에게 공격력 85％ 대미지 추가로 2턴간 아군이 입힌 대미지의 50％ 축적 축적대미지는 공격력 2691％ 까지 적용

@총력전_스킬_1404_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 목표
#DIM DYNAMIC 대미지
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트…
sTmp:0 = 「자… 심문을 시작하지.」
sTmp:1 = 「미친개의 이름을 한 번 쯤은 들어봤겠지.」
sTmp:2 = 「발키리 공안국이다! 멈춰!」
CALL SKILL_1404_BASE_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 104, RESULTS, sTmp)
; 목표 처리
CALL 총력전_시퀀스_목표선택(목표, 총력전_선택_적군)
; 목표 없으면 스킬 해재
IF 목표 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 100
대미지 = DIVIDE_ROUND(대미지, 100)
CALL 총력전_처리_피격(대미지, 목표, 시전자)
; 축적 대미지 카운터
CALL 총력전_카운터_추가(목표, 시전자, 총력전_방식_피격양, 총력전_방식_턴, 2, 1404)
; 텍스트
sTmp = %조사처리(총력전_이름:목표, "에게")% 2턴 동안 대미지 (50％) 축적 기록
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
; UI 업데이트
CALL 총력전_UI_Main(0)
WAITANYKEY

; #DIM 대미지
; 칸나최대축적대미지 = 총력전_공격력:LOCAL * 2691 / 100
; 대미지 = (총력전_공격력:LOCAL * 85 / 100) * (100 - 총력전_보스_방어력) / 100
; CALL 공격판정처리(대미지,LOCAL)
; CALL 디버프부여처리("축적",1404,2,LOCAL)


; #region == 카운터 ==
@총력전_스킬_1404_0_카운터_증가(캐릭터, 대상, 카운터번호, 증가양)
#DIM 캐릭터
#DIM 대상
#DIM 카운터번호
#DIM 증가양
총력전_카운터_증가양:캐릭터:카운터번호 += 증가양 / 2


@총력전_스킬_1404_0_카운터_종료(대상, 카운터번호)
#DIM 대상
#DIM 카운터번호

#DIM DYNAMIC 대미지
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp

; 텍스트
CALL SKILL_1404_BASE_0_NAME
sTmp = 『%RESULTS%』축적 대미지
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
; UI 업데이트
CALL 총력전_UI_Main(0)
WAITANYKEY
; 대미지
대미지 = 총력전_카운터_증가양:대상:카운터번호
iTmp = GETVAR_RAIDSTAT_SUM(총력전_카운터_시전자:대상:카운터번호, "공격력") * 2000
iTmp = DIVIDE_ROUND(iTmp, 100)
대미지 = MIN(대미지, iTmp)
; 피격
CALL 총력전_처리_피격(대미지, 대상, 총력전_카운터_시전자:대상:카운터번호, 0, 0)
; 카운터 제거
CALL 총력전_카운터_제거(대상, 카운터번호)
