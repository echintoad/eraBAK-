;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1416_SP_0_EXIST
RETURN 1

@SKILL_1416_SP_0_NAME
RESULTS = 신호탄, 발사!

;레벨은 1-5까지
@SKILL_1416_SP_0_LEVEL
RETURN 3

;대상 선택
@SKILL_1416_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_1416_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1


;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1416_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1416_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "헤헤헷! 여기서 필살기! 토왓-?! 깜짝이야⋯"

BATTLE_RATE_GRD:발동측 += LIMIT(ABL:발동자:방위 / 5, 0, 20)




@SKILL_1416_SP_0_EXPLANATION
RESULTS = 자신 부대의 받는 피해를 감소시킨다.

@SKILL_1416_SP_0_CANT_TELL


@SKILL_1416_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 500

; #region == 총력전 ==
@총력전_스킬_1416_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 4

@총력전_스킬_1416_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
; 테스트로 시전자 방어력 비례
RESULTS = 최대 세명의 아군에게 2턴간 치유력 275％ 보호막과 시전자의 방어력 25％ 만큼 방어력 증가
; RESULTS = 최대 세명의 아군에게 2턴간 치유력 275％ 쉴드 방어력 20％ 증가

@총력전_스킬_1416_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 버프대상, 3
#DIM DYNAMIC 보호막양
#DIM DYNAMIC 방어증가
#DIM CONST 지속시간 = 2
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp

; 발동 텍스트
CALL SKILL_1416_SP_0_NAME
sTmp = %총력전_이름:시전자% 스킬 시전:『%RESULTS%』
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
iTmp = 총력전_ID:시전자
IF iTmp >= 0 && NO:iTmp == 83
	; 카에데일 경우
	SELECTCASE RAND:3
		CASE 0
			sTmp = 「헤헤헷! 여기서 필살기!」
			CALL 총력전_UI_텍스트_추가(sTmp)
			sTmp = 「토왓-?! 깜짝이야…」
		CASE 1
			sTmp = 「신호탄 발사! 가라앗~!」
		CASEELSE
			sTmp = 「위험한 순간이야말로 씩씩하게!」
			CALL 총력전_UI_텍스트_추가(sTmp)
			sTmp = 「나! 등장!」
	ENDSELECT
	CALL 총력전_UI_텍스트_추가(sTmp)
ENDIF
; 목표 처리
sTmp = 버프 대상 선택
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
CALL 총력전_시퀀스_목표선택(버프대상, 총력전_선택_아군)
; 목표 없으면 스킬 해재
IF 버프대상 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 보호막양
보호막양 = GETVAR_RAIDSTAT_SUM(시전자, "치유력") * 275
보호막양 = DIVIDE_ROUND(보호막양, 100)
; 방어력양
방어증가 = GETVAR_RAIDSTAT_SUM(시전자, "방어력") * 25
방어증가 = DIVIDE_ROUND(방어증가, 100)
; 버프
VARSIZE 버프대상
FOR iTmp, 0, RESULT
	SIF 버프대상:iTmp < 0
		BREAK
	CALL 총력전_상태효과_추가(버프대상:iTmp, 시전자, 상효_종류_보호막, 보호막양, 1, 총력전_방식_지속, 총력전_방식_턴, 지속시간, 1416, 0)
	CALL 총력전_상태효과_추가(버프대상:iTmp, 시전자, 상효_종류_방어력, 방어증가, 1, 총력전_방식_지속, 총력전_방식_턴, 지속시간, 1416, 1)
	; 텍스트 추가
	sTmp = {지속시간}턴간 %조사처리(총력전_이름:(버프대상:iTmp), "의")% 보호막 추가 ({보호막양}), 방어력 증가 ({보호막양})
	CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
	; UI 업데이트
	CALL 총력전_UI_Main(0)
	WAITANYKEY
NEXT
