;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1402_SP_0_EXIST
RETURN 1

@SKILL_1402_SP_0_NAME
RESULTS = 나님의 걸작이라고!

;레벨은 1-5까지
@SKILL_1402_SP_0_LEVEL
RETURN 3

;대상 선택
@SKILL_1402_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_1402_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1


;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1402_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1402_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "자, 그러면 나님의 걸작을 시험해보도록 할까!"

LOCAL:2 = LIMIT(ABL:발동자:지략/10, 1, 15)

LOCAL:1 = RAND(LOCAL:2, 15)
FOR LOCAL, 0, LOCAL:1
	CALL DECREASE_SOLDIER(대상세력, 대상부대, ABL:발동자:지략*3/2, 1)
NEXT



@SKILL_1402_SP_0_EXPLANATION
RESULTS = 적 부대에 연속 데미지를 준다.

@SKILL_1402_SP_0_CANT_TELL


@SKILL_1402_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 350

; #region == 총력전 ==
@총력전_스킬_1402_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
RETURN 6

; alt.opt.1 대미지 확률? 실험 실패 확률
@총력전_스킬_1402_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 모든 적에게 4턴간 공격력 370％ 지속 대미지 부여

@총력전_스킬_1402_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 목표, 총력전_캐릭터수
#DIM DYNAMIC 지속대미지
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「자, 그러면 나님의 걸작을 시험해보도록 할까!」
sTmp:1 = 「후-후-후. 다녀오렴, 네즈스케!」
sTmp:2 = 「이때만을 기다리고 있었던 것이다!」
CALL SKILL_1402_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 89, RESULTS, sTmp)
; 목표 처리
CALL 총력전_목표_전체(목표, 1)
; 대미지
지속대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 370
지속대미지 = DIVIDE_ROUND(지속대미지, 100)
FOR iTmp, 0, 총력전_캐릭터수
	SIF 목표:iTmp < 0
		BREAK
	CALL 총력전_상태효과_추가_텍스트(목표:iTmp, 시전자, 상효_종류_대미지, 지속대미지, 0, 총력전_방식_턴, 총력전_방식_턴, 4, 1402)
NEXT