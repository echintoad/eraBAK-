;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_2116_SP_0_EXIST
RETURN 1

@SKILL_2116_SP_0_NAME
RESULTS = 드로잉 아트

;레벨은 1-5까지
@SKILL_2116_SP_0_LEVEL
RETURN 3

;대상 선택
@SKILL_2116_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_2116_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_2116_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_2116_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "도트 찍듯 세밀하게⋯!"
CALL DECREASE_SOLDIER(대상세력, 대상부대, 1250, 1)

@SKILL_2116_SP_0_EXPLANATION
RESULTS = 적에게 데미지를 준다.


@SKILL_2116_SP_0_CANT_TELL

; 아이가 배울수 없도록 한다
@SKILL_2116_SP_0_CANT_INHERIT


@SKILL_2116_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 400

; #region == 총력전 ==
@총력전_스킬_2116_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 3

@총력전_스킬_2116_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 무작위 적에게 5번 공격력 191％ 대미지, 모모이가 함께 있을 경우 추가로 3턴간 51％ 지속 대미지 부여

@총력전_스킬_2116_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 목표
#DIM DYNAMIC 대미지
#DIM DYNAMIC 지속대미지
#DIM DYNAMIC 모모이찾기
#DIM DYNAMIC iTmp, 2
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「원한은 없지만, 각오하도록 해.」
sTmp:1 = 「도트 찍듯 세밀하게…!」
sTmp:2 = 「연속 저격! 갑니다!」
CALL SKILL_2116_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 34, RESULTS, sTmp)
; 모모이 찾기
FOR iTmp, 0, 총력전_캐릭터수
	iTmp:1 = 총력전_ID:iTmp
	; + 팀 확인?
	IF iTmp:1 >= 0 && NO:(iTmp:1) == 33
		모모이찾기 = 1
		BREAK
	ENDIF
NEXT
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 191
대미지 = DIVIDE_ROUND(대미지, 100)
지속대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 51
지속대미지 = DIVIDE_ROUND(지속대미지, 100)
FOR iTmp, 0, 5
	; 무작위 목표
	CALL 총력전_목표_무작위(1)
	목표 = RESULT
	CALL 총력전_처리_피격(대미지, 목표, 시전자)
	IF 모모이찾기 == 1
		CALL 총력전_상태효과_추가_텍스트(목표, 시전자, 상효_종류_대미지, 지속대미지, 0, 총력전_방식_턴, 총력전_방식_턴, 3, 2116)
		; 텍스트 추가
		; sTmp = %조사처리(총력전_이름:목표, "에")% 지속 대미지 ({지속대미지})!
		; CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
		; ; UI 업데이트
		; CALL 총력전_UI_Main(0)
		; WAITANYKEY
	ENDIF
NEXT
