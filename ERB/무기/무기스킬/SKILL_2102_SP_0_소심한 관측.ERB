;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_2102_SP_0_EXIST
RETURN 1

@SKILL_2102_SP_0_NAME
RESULTS = 소심한 관측

;레벨은 1-5까지
@SKILL_2102_SP_0_LEVEL
RETURN 5

;대상 선택
@SKILL_2102_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_2102_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_2102_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_2102_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "역시 전 아무것도 못하는 쓰레기에요⋯"
BATTLE_RATE_ATC:대상측 += 10

@SKILL_2102_SP_0_EXPLANATION
RESULTS = 적 부대의 받은 데미지를 증가시킨다.


@SKILL_2102_SP_0_CANT_TELL

; 아이가 배울수 없도록 한다
@SKILL_2102_SP_0_CANT_INHERIT


@SKILL_2102_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 250

; #region == 총력전 ==
@총력전_스킬_2102_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 3

; 총력전 시작시 시전
; 테스트위해 "예상치 못한 일격"... 좀 특이하게 적용
; 나중에 바꿔야 할듯, 플래이어가 햇갈릴 확률 높을듯
@총력전_스킬_2102_초기(시전자)
#DIM 시전자
#DIMS DYNAMIC sTmp
CALL 총력전_카운터_추가(시전자, 시전자, 총력전_방식_공격, 총력전_방식_턴, 1, 2102, 0)
CALL 총력전_카운터_추가(시전자, 시전자, 총력전_방식_지속, 총력전_방식_지속, 1, 2102, 1)
; 테스트 텍스트 추가
sTmp = %총력전_이름:시전자%『예상치 못한 일격』추가
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
sTmp = 기본공격 5회 마다 턴 종료시 목표에 1턴동안 약점파악 추가
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
; UI 업데이트
CALL 총력전_UI_Main(0)
WAITANYKEY


@총력전_스킬_2102_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
; 기본 공격시 약점 파악 적용하면 2턴 으로 하던가, 적용 하자마자 효과 종료 되는거 감안 해야함
RESULTS = 적에게 공격력 424％ 대미지 추가로 1턴간 공격력 40％ 약점파악 효과를 적용. 기본공격 5회 마다 턴 종료시 목표에 1턴종안 약점파악 적용.

@총력전_스킬_2102_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 목표
#DIM DYNAMIC 대미지
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「역시 전 아무것도 못하는 쓰레기에요…」
sTmp:1 = 「나는 길가의 돌멩이…」
sTmp:2 = 「…저 같은 건 신경쓰지 말아주세요.」
CALL SKILL_2102_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 110, RESULTS, sTmp)
; 목표 처리
CALL 총력전_시퀀스_목표선택(목표, 총력전_선택_적군)
; 목표 없으면 스킬 해재
IF 목표 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 424
대미지 = DIVIDE_ROUND(대미지, 100)
CALL 총력전_처리_피격(대미지, 목표, 시전자)
; 약점 파악 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 40
대미지 = DIVIDE_ROUND(대미지, 100)
CALL 총력전_상태효과_추가_텍스트(목표, 시전자, 상효_종류_대미지, 대미지, 0, 총력전_방식_피격, 총력전_방식_턴, 1, 2102, 0)


; #region == 카운터 ==
@총력전_스킬_2102_0_카운터_증가(캐릭터, 대상, 카운터번호, 증가양)
#DIM 캐릭터
#DIM 대상
#DIM 카운터번호
#DIM 증가양

; 카운터 증가
총력전_카운터_증가양:캐릭터:카운터번호 += 증가양
; 카운터 1번 양에 대상 기록
CALL 총력전_카운터_검색_스킬번호(캐릭터, 2102, 1)
총력전_카운터_증가양:캐릭터:RESULT = 대상


@총력전_스킬_2102_0_카운터_종료(대상, 카운터번호)
#DIM 대상
#DIM 카운터번호

#DIM DYNAMIC 목표
#DIM DYNAMIC 대미지

; CALL 총력전_UI_텍스트_추가(@"증가양 {총력전_카운터_증가양:대상:카운터번호}", 칼라_파노랑)
IF 총력전_카운터_증가양:대상:카운터번호 >= 5
	총력전_카운터_증가양:대상:카운터번호 = 0
	; 카운터 1번에 기록되있는 목표 가져오기
	CALL 총력전_카운터_검색_스킬번호(대상, 2102, 1)
	목표 = 총력전_카운터_증가양:대상:RESULT
	; 대미지 처리
	대미지 = GETVAR_RAIDSTAT_SUM(대상, "공격력") * 20
	대미지 = DIVIDE_ROUND(대미지, 100)
	CALL 총력전_상태효과_추가_텍스트(목표, 대상, 상효_종류_대미지, 대미지, 0, 총력전_방식_피격, 총력전_방식_턴, 2, 2102, 1)
ENDIF
; 제거 대신, 다음 턴 종료시 또 나오게
총력전_카운터_소비양:대상:카운터번호 = 2