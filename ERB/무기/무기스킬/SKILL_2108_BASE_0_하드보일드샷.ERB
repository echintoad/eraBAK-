;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_2108_BASE_0_EXIST
RETURN 1

@SKILL_2108_BASE_0_NAME
RESULTS = 하드보일드 샷

;레벨은 1-5까지
@SKILL_2108_BASE_0_LEVEL
RETURN 3

;대상 선택
@SKILL_2108_BASE_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
;자부대 대상
RETURN 1

;발동 판정
@SKILL_2108_BASE_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;효과를 여기에 기술
@SKILL_2108_BASE_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
CALL DECREASE_SOLDIER(대상세력, 대상부대, MIN(RAND(ABL:발동자:무투*10,ABL:발동자:무투*15),RAND(ABL:발동자:야심*10,ABL:발동자:야심*15)), 1)
; SELECTCASE RAND:6
;     CASE 0
;         PRINTFORML 「한 손으로도 명중시킬 수 있어.」
;     CASE 1
;         PRINTFORML 「흐응...타겟 발견.」
;     CASE 2
;         PRINTFORML 「한 방이면 충분해.」
;     CASE 3
;         PRINTFORML 「어때? 멋지지?」
;     CASE 4
;         PRINTFORML 「처부수겠어.」
;     CASE 5
;         PRINTFORML 「아하하! 명중이야!」
; ENDSELECT

@SKILL_2108_BASE_0_EXPLANATION
RESULTS = 적을 저격하여 피해를 준다.

@SKILL_2108_BASE_0_CANT_TELL

; 아이가 배울수 없도록 한다
@SKILL_2108_BASE_0_CANT_INHERIT

@SKILL_2108_BASE_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 1000

; #region == 총력전 ==
@총력전_스킬_2108_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 4

; alt.opt.1 = 1인 대미지 + 50% 확률로 범위 추가공격
; 실패시 카운터 추가(n턴 동안 유지), 아군이 공격시 확률로 범위 추가공격 후 카운터 제거
; alt.opt.2 = 1인 대미지 + 40% 확률로 범위 추가공격
; 실패시 흥신소 캐릭터가 파티에 있으면 추가 시도 (60 ~ 80%?)
@총력전_스킬_2108_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
; 675% + 600% 범위공격)
; + 치명타확률 38％
RESULTS = 스킬 발동중 치명타 확률 38 증가. 지정된 적에게 공격력 600％ 대미지 후 1범위 안 적에게 공격력 675％ 대미지.

@총력전_스킬_2108_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 주목표
#DIM DYNAMIC 범위목표, 총력전_캐릭터수
#DIM DYNAMIC 대미지
#DIM CONST 치명타확률증가량 = 38
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「흐응~ 타겟 발견.」
sTmp:1 = 「한 손으로도 명중시킬 수 있어.」
sTmp:2 = 「해치워주지.」
CALL SKILL_2108_BASE_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 9, RESULTS, sTmp)
; 버프 추가
CALL 총력전_상태효과_추가_텍스트(시전자, 시전자, 상효_종류_치명타, 치명타확률증가량, 1, 총력전_방식_스킬, 총력전_방식_턴, 1, 2108)
; CALL 총력전_상태효과_스탯처리(시전자, 총력전_방식_스킬)
; ; 버프 텍스트
; sTmp = %조사처리(총력전_이름:시전자, "의")% 치명타 대미지 증가(30％)
; CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
; 목표 처리
CALL 총력전_시퀀스_목표선택(주목표, 총력전_선택_적군)
; 목표 없으면 스킬 해재
IF 주목표 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 600
대미지 = DIVIDE_ROUND(대미지, 100)
CALL 총력전_처리_피격(대미지, 주목표, 시전자)

; 추가 공격
CALL 총력전_목표_범위(범위목표, 주목표, 1)
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 675
대미지 = DIVIDE_ROUND(대미지, 100)
VARSIZE 범위목표
FOR iTmp, 0, RESULT
	IF 범위목표:iTmp < 0
		BREAK
	ENDIF
	CALL 총력전_처리_피격(대미지, 범위목표:iTmp, 시전자)
NEXT
