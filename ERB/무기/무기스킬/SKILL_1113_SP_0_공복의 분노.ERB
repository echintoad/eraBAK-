;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1113_SP_0_EXIST
RETURN 1

@SKILL_1113_SP_0_NAME
RESULTS = 공복의 분노

;레벨은 1-5까지
@SKILL_1113_SP_0_LEVEL
RETURN 4

;대상 선택
@SKILL_1113_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_1113_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1113_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1113_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "아직 맛 보지도 못했는데!!"
FOR LOCAL, 0, BATTLE_COMMANDER_NUM:발동측
	LOCAL:2 += ABL:(BATTLE_COMMANDER:발동측:LOCAL):요리
NEXT
LOCAL:1 = RAND(4, 8)
FOR LOCAL, 0, LOCAL:1
	CALL DECREASE_SOLDIER(대상세력, 대상부대, ABL:발동자:무투*200 / MAX(LOCAL:2 , 1), 1)
NEXT

@SKILL_1113_SP_0_EXPLANATION
RESULTS = 적 부대에 연속 데미지를 준다.


@SKILL_1113_SP_0_CANT_TELL


@SKILL_1113_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 1000

; #region == 총력전 ==
@총력전_스킬_1113_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 5

; alt.opt.1 자기 체력 비율 추가 대미지
@총력전_스킬_1113_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
; 1416
RESULTS = 적에게 공격력 472％x3 대미지. 전투 당 1회 체력이 20％ 이하 시 3턴 동안 불사 부여.

; 총력전 시작시 시전
; 배고플 땐 말 걸지 마!
@총력전_스킬_1113_초기(시전자)
#DIM 시전자

#DIMS DYNAMIC sTmp

; 카운터(무적용) 추가
CALL 총력전_카운터_추가(시전자, 시전자, 총력전_방식_피격양, 총력전_방식_지속, 1, 1113)
; 텍스트 추가
sTmp = %총력전_이름:시전자%『배고플 땐 말 걸지 마!』추가
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
sTmp = 1회 체력 20％ 이하 시 3턴동안 불사 발동.
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
; UI 업데이트
CALL 총력전_UI_Main(0)
WAITANYKEY


@총력전_스킬_1113_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 목표
#DIM DYNAMIC 대미지
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「경단~ 경단~ 으에에?!」
sTmp:1 = 「아직 맛 보지도 못했는데!!」
sTmp:2 = 「이 나쁜 놈들아아-!」
CALL SKILL_1113_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 18, RESULTS, sTmp)
; 목표 처리
CALL 총력전_시퀀스_목표선택(목표, 총력전_선택_적군)
; 목표 없으면 스킬 해재
IF 목표 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 472
대미지 = DIVIDE_ROUND(대미지, 100)
FOR iTmp, 0, 3
	CALL 총력전_처리_피격(대미지, 목표, 시전자)
NEXT


; #region == 카운터 ==
; 2턴 무적 추가
; 증가방식 피격, 소비방식 지속/0
@총력전_스킬_1113_0_카운터_증가(캐릭터, 대상, 카운터번호, 증가양)
#DIM 캐릭터
#DIM 대상
#DIM 카운터번호
#DIM 증가양

#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp

; 캐릭터 체력 20% 이상 OR 0 이면 무시
iTmp = GETVAR_RAIDSTAT_SUM(캐릭터, "최대체력") * 20
iTmp = DIVIDE_ROUND(iTmp, 100)
IF 총력전_체력:캐릭터 <= 0 || 총력전_체력:캐릭터 > iTmp
	RETURN
ENDIF

[IF_DEBUG]
sTmp = {총력전_체력:캐릭터} / {iTmp}
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_파노랑)
[ENDIF]

; 발동 텍스트
sTmp = 『배고플 땐 말 걸지 마!』
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
iTmp = 총력전_ID:캐릭터
IF iTmp >= 0 && NO:iTmp == 18
	sTmp = 「용서 못해!」
	CALL 총력전_UI_텍스트_추가(sTmp)
ENDIF
; 버프 추가 (무적=3턴)(코스트 감소=3~4감소, 2시전)
CALL 총력전_상태효과_추가_텍스트(캐릭터, 캐릭터, 상효_종류_불사, 1, 1, 총력전_방식_지속, 총력전_방식_턴, 3, 1113, 0)
CALL 총력전_상태효과_추가_텍스트(캐릭터, 캐릭터, 상효_종류_코스트감소, 4, 1, 총력전_방식_지속, 총력전_방식_스킬, 2, 1113, 1)
; 카운터 제거
CALL 총력전_카운터_제거(캐릭터, 카운터번호)