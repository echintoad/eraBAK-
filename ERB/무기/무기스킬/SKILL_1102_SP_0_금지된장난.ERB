;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1102_SP_0_EXIST
RETURN 1

@SKILL_1102_SP_0_NAME
RESULTS = 금지된 장난을 시작해볼까요?

;레벨은 1-5까지
@SKILL_1102_SP_0_LEVEL
RETURN 4

;대상 선택
@SKILL_1102_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상

RETURN 1

;발동 판정
@SKILL_1102_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1102_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1102_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "어디⋯어머?! ⋯우후훗."
; PRINTFORML 하나코의 가슴을 본 병사들이 회복되었다!
LOCAL:1 = RAND(1, 4)
LOCAL:2 = ABL:발동자:성지식
SIF LOCAL:2 == 0
LOCAL:2 = 1
FOR LOCAL, 0, LOCAL:1
	CALL INCREASE_SOLDIER(발동세력, 발동부대, RAND(LOCAL:2*100, LOCAL:2*180), 1)
NEXT


@SKILL_1102_SP_0_EXPLANATION
RESULTS = 자신 부대의 병사를 회복시킨다.

@SKILL_1102_SP_0_CANT_TELL

; 아이가 배울수 없도록 한다
@SKILL_1102_SP_0_CANT_INHERIT


@SKILL_1102_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 690

; #region == 총력전 ==
@총력전_스킬_1102_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 5

@총력전_스킬_1102_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 아군 전원에게 치유력 120％ 회복 추가로 4턴간 치유력 80％ 회복

@총력전_스킬_1102_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 대상, 총력전_캐릭터수
#DIM DYNAMIC 치유량
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「어디…어머?! …우후훗.」
sTmp:1 = 「그럼… 앙?! …후훗.」
sTmp:2 = 「제대로, 받아주셔야 해요?」
CALL SKILL_1102_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 54, RESULTS, sTmp)
; 대상 처리
CALL 총력전_목표_전체(대상, 0)
; 치유 처리
치유량 = GETVAR_RAIDSTAT_SUM(시전자, "치유력") * 120
치유량 = DIVIDE_ROUND(치유량, 100)
FOR iTmp, 0, 총력전_캐릭터수
	SIF 대상:iTmp < 0
		BREAK
	CALL 총력전_처리_치유(치유량, 대상:iTmp)
NEXT
; 지속 치유 처리, 다르게 하고 싶으면
치유량 = GETVAR_RAIDSTAT_SUM(시전자, "치유력") * 80
치유량 = DIVIDE_ROUND(치유량, 100)
FOR iTmp, 0, 총력전_캐릭터수
	SIF 대상:iTmp < 0
		BREAK
	CALL 총력전_상태효과_추가_텍스트(대상:iTmp, 시전자, 상효_종류_치유량, 치유량, 1, 총력전_방식_턴, 총력전_방식_턴, 4, 1102)
NEXT


; 총력전_하나코_힐량 = 총력전_치유력:LOCAL * 51 /100
; FOR LOCAL, 0, 총력전_파티_캐릭터수
; 	BASE:(총력전_파티_캐릭터:LOCAL):체력 += 총력전_하나코_힐량
; 	SIF BASE:(총력전_파티_캐릭터:LOCAL):체력 > MAXBASE:(총력전_파티_캐릭터:LOCAL):체력
; 	BASE:(총력전_파티_캐릭터:LOCAL):체력 = MAXBASE:(총력전_파티_캐릭터:LOCAL):체력
; 	CALL 총력전_전투_텍스트_추가(@"%조사처리(ANAME(총력전_파티_캐릭터:LOCAL),"은")% {총력전_하나코_힐량} 회복됐다!",칼라_노랑)
; 	지속치유량:LOCAL = 총력전_하나코_힐량
; 	총력전_하나코_지속힐:LOCAL = 4
; NEXT
