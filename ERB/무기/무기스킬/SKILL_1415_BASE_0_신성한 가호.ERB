;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1415_BASE_0_EXIST
RETURN 1

@SKILL_1415_BASE_0_NAME
RESULTS = 신성한 가호

;레벨은 1-5까지
@SKILL_1415_BASE_0_LEVEL
RETURN 5

;대상 선택
@SKILL_1415_BASE_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_1415_BASE_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1415_BASE_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1415_BASE_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "성스러운 힘이여."
TIMES BATTLE_DEF:발동측, 2
TIMES BATTLE_INT:발동측, 1.3

@SKILL_1415_BASE_0_EXPLANATION
RESULTS = 자신 부대의 방위와 지략을 상승시킨다.

@SKILL_1415_BASE_0_CANT_TELL


@SKILL_1415_BASE_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 700

; #region == 총력전 ==
@총력전_스킬_1415_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 2

@총력전_스킬_1415_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 대상에게 2턴간 치유력 328％ 쉴드 해로운 효과 1 제거

@총력전_스킬_1415_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 버프대상
#DIM DYNAMIC 대미지
#DIM DYNAMIC 보호막양
#DIM DYNAMIC iTmp, 2
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「성스러운 힘이여.」
sTmp:1 = 「진심으로 바랍니다…」
sTmp:2 = 「모두에게 가호를!」
CALL SKILL_1415_BASE_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 74, RESULTS, sTmp)
; 목표 처리
sTmp = 버프 대상 선택
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
CALL 총력전_시퀀스_목표선택(버프대상, 총력전_선택_아군)
; 목표 없으면 스킬 해재
IF 버프대상 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 보호막
보호막양 = GETVAR_RAIDSTAT_SUM(시전자, "치유력") * 328
보호막양 = DIVIDE_ROUND(보호막양, 100)
CALL 총력전_상태효과_추가_텍스트(버프대상, 시전자, 상효_종류_보호막, 보호막양, 1, 총력전_방식_지속, 총력전_방식_턴, 2, 1415)
; 버프 텍스트
; sTmp = %조사처리(총력전_이름:버프대상, "의")% 보호막 추가({보호막양})
; CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
; ; UI 업데이트
; CALL 총력전_UI_Main(0)
; WAITANYKEY
; 디버프 제거
CALL 총력전_상태효과_제거_디버프(버프대상)
