;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1419_SP_0_EXIST
RETURN 1

@SKILL_1419_SP_0_NAME
RESULTS = 특급송달: 전투지원품


;레벨은 1-5까지
@SKILL_1419_SP_0_LEVEL
RETURN 3

;대상 선택
@SKILL_1419_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
SIF 발동부대 < 0
	RETURN 0
RETURN 1

;발동 판정
@SKILL_1419_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
 
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1419_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1419_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
CALL INCREASE_SOLDIER(발동세력, 발동부대, ABL:발동자:지략*10, 1)

@SKILL_1419_SP_0_EXPLANATION
RESULTS = 자신 부대를 회복시킨다.

@SKILL_1419_SP_0_CANT_TELL

; 아이가 배울수 없도록 한다
@SKILL_1419_SP_0_CANT_INHERIT


@SKILL_1419_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 600

; #region == 총력전 ==
@총력전_스킬_1419_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 4

@총력전_스킬_1419_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 최대 세명의 아군에게 치유력 224％ 회복

@총력전_스킬_1419_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 치유량
#DIM DYNAMIC 대상, 3
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp
; 발동 텍스트
CALL SKILL_1419_SP_0_NAME
sTmp = %총력전_이름:시전자% 스킬 시전:『%RESULTS%』
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
iTmp = 총력전_ID:시전자
IF iTmp >= 0 && NO:iTmp == 5
	; 아야네일 경우
	SELECTCASE RAND:3
		CASE 0
			sTmp = 상황은 파악했어요. 갑니다!!
		CASE 1
			sTmp = 지원이 필요한 타이밍...! 놓치지 않아요!
		CASEELSE
			sTmp = 후방 지원, 도착했습니다!
	ENDSELECT
	CALL 총력전_UI_텍스트_추가(sTmp)
ENDIF
; 목표 처리
sTmp = 치유 대상 선택
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
CALL 총력전_시퀀스_목표선택(대상, 총력전_선택_아군)
; 목표 없으면 스킬 해재
IF 대상 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 회복 처리
치유량 = GETVAR_RAIDSTAT_SUM(시전자, "치유력") * 224
치유량 = DIVIDE_ROUND(치유량, 100)
VARSIZE 대상
FOR iTmp, 0, RESULT
	SIF 대상:iTmp < 0
		BREAK
	CALL 총력전_처리_치유(치유량, 대상:iTmp)
NEXT
