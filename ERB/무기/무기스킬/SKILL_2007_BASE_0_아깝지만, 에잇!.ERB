;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_2007_BASE_0_EXIST
RETURN 1

@SKILL_2007_BASE_0_NAME
RESULTS = 아깝지만, 에잇!

;레벨은 1-5까지
@SKILL_2007_BASE_0_LEVEL
RETURN 3

;대상 선택
@SKILL_2007_BASE_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_2007_BASE_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_2007_BASE_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_2007_BASE_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "~♪ 응? 어? 던져야 해?!"
LOCAL:1 = RAND(2, 8)
FOR LOCAL, 0, LOCAL:1
	CALL DECREASE_SOLDIER(대상세력, 대상부대, RAND(ABL:발동자:요리*3 , ABL:발동자:요리*5), 1)
NEXT
TIMES BATTLE_DEF:대상측, 0.9

@SKILL_2007_BASE_0_EXPLANATION
RESULTS = 적 부대에 연속 데미지를 주고 방어력을 감소시킨다.

@SKILL_2007_BASE_0_CANT_TELL

; 아이가 배울수 없도록 한다
@SKILL_2007_BASE_0_CANT_INHERIT


@SKILL_2007_BASE_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 400

; #region == 총력전 ==
@총력전_스킬_2007_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 3

; alt.opt.1 
@총력전_스킬_2007_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 1범위 적에게 공격력 464％ 대미지 추가로 3턴간 적의 공격 속도 25％ 감소
; 공속 감소로 변경
; RESULTS = 적에게 공격력 464％ 대미지 추가로 2턴간 적의 공격력 26％ 감소

@총력전_스킬_2007_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 주목표
#DIM DYNAMIC 범위목표, 총력전_캐릭터수
#DIM DYNAMIC 대미지
#DIM CONST 공속감소 = -25
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「흥흥~ 네? 더, 던지라고요?」
sTmp:1 = 「잘먹겠… 어? 네? 네에?」
sTmp:2 = 「가, 가라아…」
CALL SKILL_2007_BASE_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 62, RESULTS, sTmp)
; 목표 처리
CALL 총력전_시퀀스_목표선택(주목표, 총력전_선택_적군)
; 목표 없으면 스킬 해재
IF 주목표 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 범위로 변경, 적팀 1 이상이 되면 수정 필요
CALL 총력전_목표_범위(범위목표, 주목표, 1)
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 464
대미지 = DIVIDE_ROUND(대미지, 100)
FOR iTmp, 0, 총력전_캐릭터수
	SIF 범위목표:iTmp < 0
		BREAK
	CALL 총력전_처리_피격(대미지, 범위목표:iTmp, 시전자)
	CALL 총력전_상태효과_추가_텍스트(범위목표:iTmp, 시전자, 상효_종류_공격속도, 공속감소, 0, 총력전_방식_지속, 총력전_방식_턴, 3, 2007)
	; 텍스트 추가
	; sTmp = %조사처리(총력전_이름:(범위목표:iTmp), "의")% 공격 속도 ({공속감소}％) 감소!
	; CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
	; ; UI 업데이트
	; CALL 총력전_UI_Main(0)
	; WAITANYKEY
NEXT


; 공깎수치 = 총력전_보스_최초무투 * 26 / 100
; 대미지 = (총력전_공격력:LOCAL * 464 / 100) * (100 - 총력전_보스_방어력) / 100
; CALL 공격판정처리(대미지,LOCAL)
; CALL 디버프부여처리("공깎",2007,2,LOCAL,공깎수치)