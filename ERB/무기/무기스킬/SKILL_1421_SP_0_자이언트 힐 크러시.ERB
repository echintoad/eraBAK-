;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1421_SP_0_EXIST
RETURN 1

@SKILL_1421_SP_0_NAME
RESULTS = 자이언트 힐 크러시


;레벨은 1-5까지
@SKILL_1421_SP_0_LEVEL
RETURN 3

;대상 선택
@SKILL_1421_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측

RETURN 1

;발동 판정
@SKILL_1421_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
 
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1421_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1421_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측

PRINTL 적 부대의 방위가 낮아졌다!
TIMES BATTLE_방위파워:대상측:발동번호, 0.85
CALL DECREASE_SOLDIER(대상세력, 대상부대, ABL:발동자:무투*10, 1)

@SKILL_1421_SP_0_EXPLANATION
RESULTS = 적 부대에 데미지를 주고 적 부대의 방위를 낮춘다.

@SKILL_1421_SP_0_CANT_TELL

; 아이가 배울수 없도록 한다
@SKILL_1421_SP_0_CANT_INHERIT


@SKILL_1421_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 600

; #region == 총력전 ==
@총력전_스킬_1421_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 3

@총력전_스킬_1421_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 적에게 공격력 432％ 대미지 이후 3턴간 적의 방어력 37％ 감소. 3회 사용시 1범위 적에게 공격력 1204％ 대미지

@총력전_스킬_1421_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 주목표
#DIM DYNAMIC 범위목표, 총력전_캐릭터수
#DIM DYNAMIC 대미지
#DIM CONST 방어감소 = -37
#DIM DYNAMIC 카운터번호
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 4

; 발동 텍스트
sTmp:0 = 「이번 포인트는…. 여기!」
sTmp:1 = 「슈웅~ 콰앙!! 와하하핫!」
sTmp:2 = 「자, 선물이다!! 와핫핫핫하!」
sTmp:3 = 「간다간다~! 슝! 와핫핫핫하!」
CALL SKILL_1421_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 24, RESULTS, sTmp)
; 목표 처리
CALL 총력전_시퀀스_목표선택(주목표, 총력전_선택_적군)
; 목표 없으면 스킬 해재
IF 주목표 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 432
대미지 = DIVIDE_ROUND(대미지, 100)
CALL 총력전_처리_피격(대미지, 주목표, 시전자)
; 방어감소
CALL 총력전_상태효과_추가_텍스트(주목표, 시전자, 상효_종류_방어력백분율, 방어감소, 0, 총력전_방식_지속, 총력전_방식_턴, 3, 1421, 0)
; 텍스트 추가
; sTmp = %조사처리(총력전_이름:주목표, "의")% 방어력 ({방어감소}％) 감소
; CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
; ; UI 업데이트
; CALL 총력전_UI_Main(0)
; WAITANYKEY

; ----- 굴착기는 소음을 멈췄나? -----
; 카운터 없으면 추가, 있으면 양 확인
CALL 총력전_카운터_검색_스킬번호(시전자, 1421, 0)
카운터번호 = RESULT
IF 카운터번호 < 0
    ; 카운터 추가
    CALL 총력전_카운터_추가(시전자, 시전자, 총력전_방식_스킬, 총력전_방식_지속, 1, 1421)
    RETURN
ENDIF
IF 총력전_카운터_증가양:시전자:카운터번호 > 2
    sTmp = 『굴착기는 소음을 멈췄나?』 발동!
    CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
	iTmp = 총력전_ID:시전자
    IF iTmp >= 0 && NO:iTmp == 24
        sTmp = 「서프라~이즈!」
        CALL 총력전_UI_텍스트_추가(sTmp)
    ENDIF
    ; 범위로 변경, 적팀 1 이상이 되면 수정 필요
    CALL 총력전_목표_범위(범위목표, 주목표, 1, 1)
    ; 대미지
    대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 1204
    대미지 = DIVIDE_ROUND(대미지, 100)
    FOR iTmp, 0, 총력전_캐릭터수
        SIF 범위목표:iTmp < 0
            BREAK
        CALL 총력전_처리_피격(대미지, 범위목표:iTmp, 시전자)
    NEXT
    ; 카운터 제거
    CALL 총력전_카운터_제거(시전자, 카운터번호)
ENDIF


; #region == 카운터 ==
