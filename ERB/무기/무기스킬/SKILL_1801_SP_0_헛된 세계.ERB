;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1801_SP_0_EXIST
RETURN 1

@SKILL_1801_SP_0_NAME
RESULTS = 헛된 세계

;레벨은 1-5까지
@SKILL_1801_SP_0_LEVEL
RETURN 3

;대상 선택
@SKILL_1801_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_1801_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1801_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1801_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "먼지는 먼지로."
LOCAL:1 = RAND(3, 7)
FOR LOCAL, 0, LOCAL:1
	CALL DECREASE_SOLDIER(대상세력, 대상부대, ABL:발동자:무투*3, 1)
NEXT


@SKILL_1801_SP_0_EXPLANATION
RESULTS = 적 부대에 연속 데미지를 준다.


@SKILL_1801_SP_0_CANT_TELL


@SKILL_1801_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 400

; #region == 총력전 ==
@총력전_스킬_1801_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
RETURN 5

; alt.opt.1 적 지형에 "엄폐물" 추가, 턴 시작 -> 주변에 지속 대미지 추가
; 에폐물 시스템을 좀 손 볼필요 있음, 같은 자리에 엄폐물 여러게 있어도 괜찬은지
; alt.opt.2 지속 대미지 후 시전자 공격력 증가
@총력전_스킬_1801_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
; 1범위로?
RESULTS = 적에게 7턴간 공격력 200％ 지속 대미지 부여. 시작시 팀에 자신 제외 아리우스 스쿼드 출신 학생당 공격력 증가 (8 + 학생수 x 15).
; RESULTS = 적에게 7턴간 공격력 180％ 지속 대미지 부여

; 총력전 시작시 시전
; 스쿼드 멤버당 공격력 증가, 사오리는 치명 대미지로 변경
@총력전_스킬_1801_초기(시전자)
#DIM 시전자

#DIM DYNAMIC 스쿼드수
#DIM DYNAMIC 스쿼드번호 = 113, 114, 115, 116, 55
#DIM DYNAMIC 공격증가
#DIM DYNAMIC iTmp, 2
#DIMS DYNAMIC sTmp

; 스쿼드 학생 확인
FOR iTmp, 0, 총력전_캐릭터수
	iTmp:1 = 총력전_ID:iTmp
	; 없거나 시전자면 스킵
	SIF iTmp:1 < 0 || iTmp == 시전자
		CONTINUE
	IF MATCH(스쿼드번호, NO:(iTmp:1))
		스쿼드수 ++
	ENDIF
NEXT
IF 스쿼드수 > 0
	공격증가 = 스쿼드수 * 15 + 8
	CALL 총력전_상태효과_추가(시전자, 시전자, 상효_종류_공격력백분율, 공격증가, 2, 총력전_방식_지속, 총력전_방식_지속, 1, 1801, 0)
	; 테스트 텍스트 추가
	sTmp = %총력전_이름:시전자%『아리우스 스쿼드』
	CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
	sTmp = 공격력 증가 ({공격증가}％)
	CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
	; UI 업데이트
	CALL 총력전_UI_Main(0)
	WAITANYKEY
ENDIF


@총력전_스킬_1801_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 목표
#DIM DYNAMIC 지속대미지
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「…전부 무의미해.」
sTmp:1 = 「모든 것은 헛될 뿐.」
sTmp:2 = 「먼지는 먼지로.」
CALL SKILL_1801_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 115, RESULTS, sTmp)
; 목표 처리
CALL 총력전_시퀀스_목표선택(목표, 총력전_선택_적군)
; 목표 없으면 스킬 해재
IF 목표 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 지속 대미지
지속대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 200
지속대미지 = DIVIDE_ROUND(지속대미지, 100)
CALL 총력전_상태효과_추가_텍스트(목표, 시전자, 상효_종류_대미지, 지속대미지, 0, 총력전_방식_턴, 총력전_방식_턴, 7, 1801, 1)




; 미사키화상대미지 = (총력전_공격력:LOCAL * 180 / 100)
; CALL 디버프부여처리("화상",1801,7,LOCAL,미사키화상대미지)