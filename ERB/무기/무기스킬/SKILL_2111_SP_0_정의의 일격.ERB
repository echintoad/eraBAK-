;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_2111_SP_0_EXIST
RETURN 1

@SKILL_2111_SP_0_NAME
RESULTS = 정의의 일격

;레벨은 1-5까지
@SKILL_2111_SP_0_LEVEL
RETURN 4

;대상 선택
@SKILL_2111_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
SIF BATTLE_COMMANDER_NUM:대상측 == 0
	RETURN 0
RETURN 1


;발동 판정
@SKILL_2111_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
SIF BATTLE_COMMANDER_NUM:대상측 == 0
	RETURN 0
RETURN 1


;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_2111_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_2111_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "탄환장전, 조준 양호...사격개시!"
LOCAL = BATTLE_COMMANDER_NUM:대상측
TARGET = RAND:LOCAL
IF RAND:2 == 0
	PRINTFORML %ANAME(BATTLE_COMMANDER:대상측:TARGET)%는 피격되었다!
	PRINTFORML 이 전투에 참가 할 수 없다!
	CALL BATTLE_KNOCKOUT(대상측, TARGET)
ENDIF


@SKILL_2111_SP_0_EXPLANATION
RESULTS = 적 한명을 저격해 행동 불능으로 만든다.

@SKILL_2111_SP_0_CANT_TELL


@SKILL_2111_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 550

; #region == 총력전 ==
@총력전_스킬_2111_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 3

; alt.opt.1 명중 하면 추가공격?
@총력전_스킬_2111_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 적에게 공격력 664％ 대미지, 75％ 확률로 공격력 716％ 추가 대미지
; RESULTS = 적에게 공격력 664％ 대미지 75％ 확률로 공격력 1380％ 대미지

@총력전_스킬_2111_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 목표
#DIM DYNAMIC 대미지
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
CALL SKILL_2111_SP_0_NAME
sTmp = %총력전_이름:시전자% 스킬 시전:『%RESULTS%』
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
iTmp = 총력전_ID:시전자
IF iTmp >= 0 && NO:iTmp == 1
	; 시로코/쿠로코일 경우
	SELECTCASE RAND:3
		CASE 0
			sTmp = 「탄환장전, 조준 양호...」
			CALL 총력전_UI_텍스트_추가(sTmp)
			sTmp = 「사격개시!」
		CASE 1
			sTmp = 「목표를 확인.」
			CALL 총력전_UI_텍스트_추가(sTmp)
			sTmp = 「배제합니다.」
		CASEELSE
			sTmp = 「파이어.」
	ENDSELECT
	CALL 총력전_UI_텍스트_추가(sTmp)
ENDIF
; 목표 처리
CALL 총력전_시퀀스_목표선택(목표, 총력전_선택_적군)
; 목표 없으면 스킬 해재
IF 목표 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 664
대미지 = DIVIDE_ROUND(대미지, 100)
CALL 총력전_처리_피격(대미지, 목표, 시전자)
; 명중 하면
IF RESULT == 1 && RAND:4 > 0
	; 75% 추가 대미지
	대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 716
	대미지 = DIVIDE_ROUND(대미지, 100)
	CALL 총력전_처리_피격(대미지, 목표, 시전자)
ENDIF


; SELECTCASE RAND:4
; CASE 0
; 대미지 = (총력전_공격력:LOCAL * 664 / 100) * (100 - 총력전_보스_방어력) / 100
; CASEELSE
; 대미지 = (총력전_공격력:LOCAL * 1380 / 100) * (100 - 총력전_보스_방어력) / 100
; ENDSELECT
; CALL 공격판정처리(대미지,LOCAL)

