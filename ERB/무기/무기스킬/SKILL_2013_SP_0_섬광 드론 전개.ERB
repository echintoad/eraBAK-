;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_2013_SP_0_EXIST
RETURN 1

@SKILL_2013_SP_0_NAME
RESULTS = 섬광 드론 전개

;레벨은 1-5까지
@SKILL_2013_SP_0_LEVEL
RETURN 4

;대상 선택
@SKILL_2013_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_2013_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
SIF BATTLE_COMMANDER_NUM:대상측 == 0
	RETURN 0
COMBAT_SKILL_TARGET = RAND:(BATTLE_COMMANDER_NUM:대상측)
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_2013_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_2013_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "드론 가동. 엄호 부탁드립니다."
BATTLE_SKILL_SEALED:대상측:COMBAT_SKILL_TARGET = 1

@SKILL_2013_SP_0_EXPLANATION
RESULTS = 적 한 사람의 스킬을 봉인한다.


@SKILL_2013_SP_0_CANT_TELL

; 아이가 배울수 없도록 한다
@SKILL_2013_SP_0_CANT_INHERIT


@SKILL_2013_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 800

; #region == 총력전 ==
@총력전_스킬_2013_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 3

@총력전_스킬_2013_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 적에게 공격력 545％ 대미지와 군중제어 400 추가, 2턴간 자신의 방어력 36％ 증가
; RESULTS = 적에게 공격력 545％ 대미지와 1턴간 기절 추가로 2턴간 자신의 방어력 36％ 증가

@총력전_스킬_2013_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 목표
#DIM DYNAMIC 대미지
#DIM DYNAMIC 군중제어
#DIM CONST 방어증가 = 36
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 4

; 발동 텍스트
sTmp:0 = 「감도 양호. 돌입을 개시합니다.」
sTmp:1 = 「드론 가동. 엄호 부탁드립니다.」
sTmp:2 = 「전원, 충격에 주의하세요!」
CALL SKILL_2013_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 107, RESULTS, sTmp)
; 목표 처리
CALL 총력전_시퀀스_목표선택(목표, 총력전_선택_적군)
; 목표 없으면 스킬 해재
IF 목표 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 412
대미지 = DIVIDE_ROUND(대미지, 100)
CALL 총력전_처리_피격(대미지, 목표, 시전자)
; 명중 하면 군중제어 적용
IF RESULT == 1
	군중제어 = 800 * GETVAR_RAIDSTAT_SUM(시전자, "군중제어강화")
	군중제어 = DIVIDE_ROUND(군중제어, 100)
	CALL 총력전_처리_군중제어(군중제어, 목표, 시전자)
ENDIF
; 방어 증가, 받는 대미지 감소로 변경? 피해감소백분율?
CALL 총력전_상태효과_추가_텍스트(시전자, 시전자, 상효_종류_방어력백분율, 방어증가, 1, 총력전_방식_지속, 총력전_방식_턴, 2, 2013)




; #DIM 방어력증가량
; 방어력증가량 = 36
; 대미지 = (총력전_공격력:LOCAL * 412 / 100) * (100 - 총력전_보스_방어력) / 100
; CALL 공격판정처리(대미지,LOCAL)
; CALL 버프처리(1,,,,,,,,,,방어력증가량,,,1908,,LOCAL)
; CALL 디버프부여처리("기절",2013,1,LOCAL)