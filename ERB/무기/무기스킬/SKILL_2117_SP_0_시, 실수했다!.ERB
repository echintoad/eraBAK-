;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_2117_SP_0_EXIST
RETURN 1

@SKILL_2117_SP_0_NAME
RESULTS = 시, 실수했다!

;레벨은 1-5까지
@SKILL_2117_SP_0_LEVEL
RETURN 3

;대상 선택
@SKILL_2117_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
SIF BATTLE_COMMANDER_NUM:대상측 == 0
	RETURN 0
RETURN 1

;발동 판정
@SKILL_2117_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
SIF BATTLE_COMMANDER_NUM:대상측 == 0
	RETURN 0
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_2117_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_2117_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측

LOCAL:1 = BATTLE_COMMANDER_NUM:대상측

FOR LOCAL, 0, LOCAL:1
	CALL DECREASE_SOLDIER(대상세력, 대상부대, MAX(RAND(ABL:발동자:무투*10 , ABL:발동자:무투*15) , RAND(ABL:발동자:야심*10 , ABL:발동자:야심*15)), 1)
NEXT
@SKILL_2117_SP_0_EXPLANATION
RESULTS = 적에게 데미지를 준다. 상대 지휘관이 많을수록 추가 피해를 준다.


@SKILL_2117_SP_0_CANT_TELL


@SKILL_2117_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 400

; #region == 총력전 ==
@총력전_스킬_2117_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 5

@총력전_스킬_2117_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
; RESULTS = 모든 적에게 공격력 987％ 대미지 이후 4턴간 적의 방어력 15％ 감소
; 실제 확률 = 40%
RESULTS = 모든 적에게 공격력 987％ 대미지, 50％ 확률로 시전자도 80％ 대미지 피해. 이후 4턴간 적의 방어력 15％ 감소.

@총력전_스킬_2117_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 목표, 총력전_캐릭터수
#DIM DYNAMIC 대미지
#DIM CONST 방감수치 = -15
#DIM DYNAMIC iTmp, 2
#DIMS DYNAMIC sTmp, 3
; 발동 텍스트
sTmp:0 = 「키히히힛! 쏴라!」
sTmp:1 = 「키키키킷! 전탄 발사!」
sTmp:2 = 「가라! 가라! 가라! 캬하하하하!」
CALL SKILL_2117_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 6, RESULTS, sTmp)
; 목표 처리
CALL 총력전_목표_전체(목표, 1)
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 987
대미지 = DIVIDE_ROUND(대미지, 100)
VARSIZE 목표
FOR iTmp, 0, RESULT
	SIF 목표:iTmp < 0
		BREAK
	CALL 총력전_처리_피격(대미지, 목표:iTmp, 시전자)
	; 방어 감소
	CALL 총력전_상태효과_추가_텍스트(목표:iTmp, 시전자, 상효_종류_방어력백분율, 방감수치, 0, 총력전_방식_지속, 총력전_방식_턴, 4, 2117)
NEXT
; 40% 확률로 시전자 피해
iTmp = RAND:5
IF iTmp < 2
	; 마코토일 경우 추가 텍스트
	iTmp:1 = 총력전_ID:시전자
	IF iTmp:1 >= 0 && NO:(iTmp:1) == 6
		SELECTCASE RAND:3
			CASE 0
				sTmp = 「…어? 거, 거꾸로라고?」
			CASE 1
				sTmp = 「…응? 바, 반대?」
			CASEELSE
				sTmp = 「…엇, 여, 여기가 아니야?!」
		ENDSELECT
	ENDIF
	CALL 총력전_UI_텍스트_추가(sTmp)
	대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 80
	대미지 = DIVIDE_ROUND(대미지, 100)
	CALL 총력전_처리_피격(대미지, 시전자)
ENDIF