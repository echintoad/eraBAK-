;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_2014_SP_0_EXIST
RETURN 1

@SKILL_2014_SP_0_NAME
RESULTS = 이것도 나의 힘

;레벨은 1-5까지
@SKILL_2014_SP_0_LEVEL
RETURN 5

;대상 선택
@SKILL_2014_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_2014_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
SIF 발동부대 < 0
	RETURN 0
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_2014_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_2014_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "이걸로, 도움이 될 수 있다면⋯"
UNIT_TIRED_COUNT:발동세력:발동부대 = MAX(UNIT_TIRED_COUNT:발동세력:발동부대 - RAND(3, 6), 0)
CALL INCREASE_SOLDIER(발동세력, 발동부대, RAND(ABL:발동자:방위*5, ABL:발동자:방위*10), 1)

@SKILL_2014_SP_0_EXPLANATION
RESULTS = 자부대의 피로도를 경감시키고 인원을 보충한다.


@SKILL_2014_SP_0_CANT_TELL


@SKILL_2014_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 500

; #region == 총력전 ==
@총력전_스킬_2014_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
RETURN 4

@총력전_스킬_2014_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
; 카운터로 범위 힐 테스트
; 치유력, 회피율, 체력 중 하나로
RESULTS = 3턴동안 턴 종료시 2범위내 아군에게 치유력 120％ 회복. 시작시 팀에 자신 제외 아리우스 스쿼드 출신 학생당 회피율 증가 (학생수 x 10).
; RESULTS = 아군 전원에게 3턴간 치유력 73％ 회복

; 총력전 시작시 시전
; 스쿼드 멤버당 공격력 증가, 사오리는 치명 대미지로 변경
@총력전_스킬_2014_초기(시전자)
#DIM 시전자

#DIM DYNAMIC 스쿼드수
#DIM DYNAMIC 스쿼드번호 = 113, 114, 115, 116, 55
#DIM DYNAMIC 회피증가
#DIM DYNAMIC iTmp, 2
#DIMS DYNAMIC sTmp

; 스쿼드 학생 확인
FOR iTmp, 0, 총력전_캐릭터수
	iTmp:1 = 총력전_ID:iTmp
	; 없거나 시전자면 스킵
	SIF iTmp:1 < 0 || iTmp == 시전자
		CONTINUE
	IF MATCH(스쿼드번호, NO:(iTmp:1))
		스쿼드수 ++
	ENDIF
NEXT
IF 스쿼드수 > 0
	회피증가 = 스쿼드수 * 10
	CALL 총력전_상태효과_추가(시전자, 시전자, 상효_종류_회피율백분율, 회피증가, 2, 총력전_방식_지속, 총력전_방식_지속, 1, 1801, 0)
	; 테스트 텍스트 추가
	sTmp = %총력전_이름:시전자%『아리우스 스쿼드』
	CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
	sTmp = 회피율 증가 ({회피증가}％)
	CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
	; UI 업데이트
	CALL 총력전_UI_Main(0)
	WAITANYKEY
ENDIF

@총력전_스킬_2014_발동(시전자)
#DIM 시전자

#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「이것은, 나를 위해…」
sTmp:1 = 「그럼 부탁할게.」
sTmp:2 = 「이걸로, 도움이 될 수 있다면…」
CALL SKILL_2014_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 114, RESULTS, sTmp)
; 힐 카운터
CALL 총력전_카운터_추가(시전자, 시전자, 총력전_방식_턴, 총력전_방식_턴, 3, 2014)
sTmp = 3턴동안 %조사처리(총력전_이름:시전자, "의")% 2범위내 아군 치유
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
; UI 업데이트
CALL 총력전_UI_Main(0)
WAITANYKEY

; #region == 카운터 ==
@총력전_스킬_2014_0_카운터_증가(캐릭터, 대상, 카운터번호, 증가양)
#DIM 캐릭터
#DIM 대상
#DIM 카운터번호
#DIM 증가양

; #DIM DYNAMIC 주대상
#DIM DYNAMIC 치유대상, 총력전_캐릭터수
#DIM DYNAMIC 치유량
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp

; 범위 대상, 적팀 1 이상이 되면 수정 필요
CALL 총력전_목표_범위(치유대상, 캐릭터, 0, 2)
; 치유 처리
치유량 = GETVAR_RAIDSTAT_SUM(캐릭터, "치유력") * 120
치유량 = DIVIDE_ROUND(치유량, 100)
FOR iTmp, 0, 총력전_캐릭터수
	SIF 치유대상:iTmp < 0
		BREAK
	CALL 총력전_처리_치유(치유량, 치유대상:iTmp)
NEXT