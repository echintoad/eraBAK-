;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1904_SP_0_EXIST
RETURN 1

@SKILL_1904_SP_0_NAME
RESULTS = 불굴의 의지

;레벨은 1-5까지
@SKILL_1904_SP_0_LEVEL
RETURN 4

;대상 선택
@SKILL_1904_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상

RETURN 1

;발동 판정
@SKILL_1904_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
 
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1904_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1904_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "인터프리터 기동. 해석 개시."
BATTLE_RATE_GRD:발동측 += (ABL:발동자:방위 / 3)

@SKILL_1904_SP_0_EXPLANATION
RESULTS = 자신 부대의 받는 피해를 감소시킨다.

@SKILL_1904_SP_0_CANT_TELL

; 아이가 배울수 없도록 한다
@SKILL_1904_SP_0_CANT_INHERIT


@SKILL_1904_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 400

; #region == 총력전 ==
@총력전_스킬_1904_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 4

; 힐 좀 강한거 같아서 쿨 3~4?
@총력전_스킬_1904_쿨다운(가변)
#DIM 가변
RETURN 4

@총력전_스킬_1904_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 잃은체력의 30％ 회복후 2턴간 자신에게 치유력 50％ 회복

@총력전_스킬_1904_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 치유량
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「패턴 분석 중…」
sTmp:1 = 「디펜스 모드 전환.」
sTmp:2 = 「적용 완료. 이제 괜찮아.」
CALL SKILL_1904_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 49, RESULTS, sTmp)
; 30% 치유 처리
치유량 = (GETVAR_RAIDSTAT_SUM(시전자, "최대체력") - 총력전_체력:시전자) * 3
치유량 = DIVIDE_ROUND(치유량, 10)
[IF_DEBUG]
sTmp = {총력전_체력:시전자} / {GETVAR_RAIDSTAT_SUM(시전자, "최대체력")}
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_파노랑)
sTmp = 치유량:{치유량}
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_파노랑)
[ENDIF]
IF 치유량 > 0
	CALL 총력전_처리_치유(치유량, 시전자)
ENDIF
; 지속치유 처리
치유량 = GETVAR_RAIDSTAT_SUM(시전자, "치유력") * 50
치유량 = DIVIDE_ROUND(치유량, 100)
CALL 총력전_상태효과_추가_텍스트(시전자, 시전자, 상효_종류_치유량, 치유량, 1, 총력전_방식_턴, 총력전_방식_턴, 2, 1904)
; 텍스트 추가
; sTmp = 2턴간 %조사처리(총력전_이름:시전자, "가")% 지속 치유 ({치유량})!
; CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
; ; UI 업데이트
; CALL 총력전_UI_Main(0)
; WAITANYKEY