;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1109_SP_0_EXIST
RETURN 1

@SKILL_1109_SP_0_NAME
RESULTS = 갈게!

;레벨은 1-5까지
@SKILL_1109_SP_0_LEVEL
RETURN 2

;대상 선택
@SKILL_1109_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상

RETURN 1

;발동 판정
@SKILL_1109_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
 
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1109_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1109_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "아스나, 간다-!"
TIMES BATTLE_무투파워:발동측:발동번호, 1.3
CAPTURE_RATE:발동번호 = MIN_DECREASE(CAPTURE_RATE:발동번호, 200, 0)

@SKILL_1109_SP_0_EXPLANATION
RESULTS = 자신의 무투를 증가시키고 포박될 확률을 줄인다.


@SKILL_1109_SP_0_CANT_TELL


@SKILL_1109_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 800


; #region == 총력전 ==
@총력전_스킬_1109_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 2

@총력전_스킬_1109_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 이동, 자신과 목표 지점 캐릭터 보스 조준 회피 (50％ 확률), 3턴간 자신에 회피율 (30％) 공격속도 증가 (60％)
; RESULTS = 1턴간 회피율 43％ 증가 추가로 3턴간 공격속도 57％ 증가

@총력전_스킬_1109_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 시작위치 ; 이동전 위치, 보스 회피용
#DIM CONST 회피증가 = 30
#DIM CONST 공속증가 = 60
#DIM DYNAMIC iTmp, 2
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「아스나! 갑니다!~」
sTmp:1 = 「잇츠 쇼타임!」
sTmp:2 = 「아스나! 여기에 등장!」
CALL SKILL_1109_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 42, RESULTS, sTmp)
; 위치 기록
시작위치 = 총력전_위치:시전자
; 이동
CALL 총력전_시퀀스_이동_시전자(시전자)
; 시전자 보스 조준 회피
IF RAND:2 == 0
	[IF_DEBUG]
	sTmp = ({시전자}) 시전자 회피 시도
	CALL 총력전_UI_텍스트_추가(sTmp, 칼라_파노랑)
	[ENDIF]
	CALL 총력전_목표_조준_취소(시전자)
ENDIF
; 시작위치 캐릭터 확인후 조준 회피
iTmp = FINDELEMENT(총력전_위치, 시작위치)
[IF_DEBUG]
sTmp = 시작위치 캐릭터:%총력전_이름:iTmp%
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_파노랑)
[ENDIF]
iTmp:1 = 총력전_캐릭터상태(iTmp)
; 나중에 "총력전_목표_피아식별" 로 변경
IF iTmp:1 > 0 && iTmp:1 != 총력전_상태_행동불능 && 총력전_팀:iTmp == 총력전_팀:시전자
	IF RAND:2 == 0
		[IF_DEBUG]
		sTmp = ({iTmp}) 시작위치 캐릭터 회피 시도
		CALL 총력전_UI_텍스트_추가(sTmp, 칼라_파노랑)
		[ENDIF]
		CALL 총력전_목표_조준_취소(iTmp)
	ENDIF
ENDIF
; 버프
CALL 총력전_상태효과_추가(시전자, 시전자, 상효_종류_회피율백분율, 회피증가, 1, 총력전_방식_지속, 총력전_방식_턴, 5, 2103, 0)
CALL 총력전_상태효과_추가(시전자, 시전자, 상효_종류_공격속도, 공속증가, 1, 총력전_방식_지속, 총력전_방식_턴, 5, 2103, 1)
; 텍스트 추가
sTmp = 5턴 동안 %조사처리(총력전_이름:시전자, "의")% 회피율 ({회피증가}), 공격 속도 증가 ({공속증가}％)
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
; UI 업데이트
CALL 총력전_UI_Main(0)
WAITANYKEY

; 공격속도증가량 = 57
; 회피율증가량 = 43
; CALL 버프처리(2,공격속도증가량,,,,,,,,회피율증가량,,,,1109,,LOCAL)

; @총력전_스킬_1109_버프지속
; RETURN 3
