;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1908_SP_0_EXIST
RETURN 1

@SKILL_1908_SP_0_NAME
RESULTS = 펌프패닝

;레벨은 1-5까지
@SKILL_1908_SP_0_LEVEL
RETURN 3

;대상 선택
@SKILL_1908_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_1908_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1908_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1908_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "죽어주세요죽어주세요죽어주세요!!!"
LOCAL:1 = RAND(1, 6)
FOR LOCAL, 0, LOCAL:1
	CALL DECREASE_SOLDIER(대상세력, 대상부대, ABL:발동자:방위*10, 1)
NEXT

@SKILL_1908_SP_0_EXPLANATION
RESULTS = 적 부대에 연속 데미지를 준다.


@SKILL_1908_SP_0_CANT_TELL

; 아이가 배울수 없도록 한다
@SKILL_1908_SP_0_CANT_INHERIT


@SKILL_1908_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 200

; #region == 총력전 ==
@총력전_스킬_1908_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 4

@총력전_스킬_1908_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
; 963 = 107 * 9
RESULTS = 지정된 적과 1범위 안 적에게 공격력 107％x9 대미지 추가로 2턴간 자신의 방어력 36％ 증가

@총력전_스킬_1908_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 주목표 ; 목표
#DIM DYNAMIC 목표, 총력전_캐릭터수 ; 목표 + 주변 적
#DIM DYNAMIC 대미지
#DIM DYNAMIC 방어증가백분율 = 36
#DIM iTmp, 2
#DIMS sTmp, 3

; 발동 텍스트
sTmp:0 = 「가, 갑니다!」
sTmp:1 = 「우, 우와아아아아-앗!」
sTmp:2 = 「죽어주세요죽어주세요죽어주세요!」
CALL SKILL_1908_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 12, RESULTS, sTmp)
; 목표 처리
CALL 총력전_시퀀스_목표선택(주목표, 총력전_선택_적군)
; 목표 없으면 스킬 해재
IF 주목표 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 범위로 변경, 적팀 1 이상이 되면 수정 필요
CALL 총력전_목표_범위(목표, 주목표, 1)
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 107
대미지 = DIVIDE_ROUND(대미지, 100)
FOR iTmp:1, 0, 9
	; 범위로 변경
	FOR iTmp, 0, 총력전_캐릭터수
		IF 목표:iTmp < 0
			BREAK
		ENDIF
		CALL 총력전_처리_피격(대미지, 목표:iTmp, 시전자)
	NEXT
NEXT
; 방어 버프
CALL 총력전_상태효과_추가_텍스트(시전자, 시전자, 상효_종류_방어력백분율, 방어증가백분율, 1, 총력전_방식_지속, 총력전_방식_턴, 2, 1908)
; 버프 텍스트
; sTmp = %조사처리(총력전_이름:시전자, "의")% 방어력 증가({방어증가백분율}％)
; CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)