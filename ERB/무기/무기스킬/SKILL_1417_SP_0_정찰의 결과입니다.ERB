;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1417_SP_0_EXIST
RETURN 1

@SKILL_1417_SP_0_NAME
RESULTS = 정찰의 결과입니다

;레벨은 1-5까지
@SKILL_1417_SP_0_LEVEL
RETURN 4

;대상 선택
@SKILL_1417_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
SIF BATTLE_COMMANDER_NUM:발동측 == 0
	RETURN 0
COMBAT_SKILL_TARGET = RAND:(BATTLE_COMMANDER_NUM:발동측)
;자신한테는 발동 안하게 했는데 왜 하지?
SIF COMBAT_SKILL_TARGET == 발동자
	RETURN 0
RETURN 1

;발동 판정
@SKILL_1417_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
 
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1417_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1417_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "선도부의 정보수집능력, 보여드리죠!"

TIMES BATTLE_무투파워:발동측:발동번호, 1.15
BATTLE_SKILL_RATE:발동측:COMBAT_SKILL_TARGET += 10

PRINTL 자신 부대의 무투가 증가했다!
PRINTFORML %ANAME(BATTLE_COMMANDER:발동측:COMBAT_SKILL_TARGET)%의 스킬 발동률이 올랐다!
@SKILL_1417_SP_0_EXPLANATION
RESULTS = 자신 부대의 무투를 증가시키고 스킬 발동을 쉽게 한다.


@SKILL_1417_SP_0_CANT_TELL

; 아이가 배울수 없도록 한다
@SKILL_1417_SP_0_CANT_INHERIT


@SKILL_1417_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 500

; #region == 총력전 ==
@총력전_스킬_1417_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 3

@총력전_스킬_1417_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 대상에게 2턴간 치명타 수치 40, 치명타 대미지 73％ 증가

@총력전_스킬_1417_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 버프대상 ; 버프 대상
#DIM CONST 치명타확률증가량 = 40
#DIM CONST 치명타대미지증가량 = 73
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「선도부의 정보수집능력, 보여드리죠!」
sTmp:1 = 「이후는 부탁드리겠습니다.」
sTmp:2 = 「후후, 준비는 완벽하니까요.」
CALL SKILL_1417_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 14, RESULTS, sTmp)
; 목표 처리
sTmp = 버프 대상 선택
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
CALL 총력전_시퀀스_목표선택(버프대상, 총력전_선택_아군)
; 목표 없으면 스킬 해재
IF 버프대상 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 버프
CALL 총력전_상태효과_추가(버프대상, 시전자, 상효_종류_치명타, 치명타확률증가량, 1, 총력전_방식_지속, 총력전_방식_턴, 2, 1417, 0)
CALL 총력전_상태효과_추가(버프대상, 시전자, 상효_종류_치명타대미지, 치명타대미지증가량, 1, 총력전_방식_지속, 총력전_방식_턴, 2, 1417, 1)
; 텍스트 추가
sTmp = 2턴간 %조사처리(총력전_이름:버프대상, "의")% 치명타 ({치명타확률증가량}), 치명타 대미지 ({치명타대미지증가량}％) 증가!
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
; UI 업데이트
CALL 총력전_UI_Main(0)
WAITANYKEY