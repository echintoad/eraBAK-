;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_2015_SP_0_EXIST
RETURN 1

@SKILL_2015_SP_0_NAME
RESULTS = 천하일미!

;레벨은 1-5까지
@SKILL_2015_SP_0_LEVEL
RETURN 4

;대상 선택
@SKILL_2015_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_2015_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1


;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_2015_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_2015_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
LOCAL:1 = RAND(1, 3)
FOR LOCAL, 0, LOCAL:1
	CALL INCREASE_SOLDIER(대상세력, 대상부대, ABL:발동자:요리 * 7, 1)
NEXT


@SKILL_2015_SP_0_EXPLANATION
RESULTS = 자신 부대의 병력을 요리에 비례해 회복시킨다.

@SKILL_2015_SP_0_CANT_TELL

; 아이가 배울수 없도록 한다
@SKILL_2015_SP_0_CANT_INHERIT


@SKILL_2015_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 250

; #region == 총력전 ==
@총력전_스킬_2015_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 4

; 테스트 위해 무작위 아군으로 튕기기
@총력전_스킬_2015_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 아군에게 치유력 250％ 회복 해로운 효과 1 제거 이후 아군 3명 에게 튕기면서 100％ 추가 회복
; RESULTS = 아군 전원에게 치유력 166％ 회복 해로운 효과 모두 제거

@총력전_스킬_2015_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 치유량
#DIM DYNAMIC 치유대상
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「영차…! 네! 드세요.」
sTmp:1 = 「자, 완성… 이얍!」
sTmp:2 = 「좋아, 됐어! 많~이 먹어!」
CALL SKILL_2015_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 93, RESULTS, sTmp)
; 목표 처리
sTmp = 치유 대상 선택
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
CALL 총력전_시퀀스_목표선택(치유대상, 총력전_선택_아군)
; 목표 없으면 스킬 해재
IF 치유대상 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 치유
치유량 = GETVAR_RAIDSTAT_SUM(시전자, "치유력") * 250
치유량 = DIVIDE_ROUND(치유량, 100)
CALL 총력전_처리_치유(치유량, 치유대상, 시전자)
; 튕기기 치유
치유량 = GETVAR_RAIDSTAT_SUM(시전자, "치유력") * 100
치유량 = DIVIDE_ROUND(치유량, 100)
FOR iTmp, 0, 3
	CALL 총력전_목표_무작위(0)
	CALL 총력전_처리_치유(치유량, RESULT, 시전자)
NEXT
