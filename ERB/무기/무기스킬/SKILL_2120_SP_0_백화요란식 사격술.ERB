;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_2120_SP_0_EXIST
RETURN 1

@SKILL_2120_SP_0_NAME
RESULTS = 백화요란식 사격술

;레벨은 1-5까지
@SKILL_2120_SP_0_LEVEL
RETURN 4

;대상 선택
@SKILL_2120_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_2120_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_2120_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_2120_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측

CALL DECREASE_SOLDIER(대상세력, 대상부대,RAND(ABL:발동자:무투*15 , ABL:발동자:무투*30), 1)
BATTLE_RATE_ATC:대상측 += 10

@SKILL_2120_SP_0_EXPLANATION
RESULTS = 적 부대에 데미지를 주고 받는 피해를 증가시킨다.


@SKILL_2120_SP_0_CANT_TELL


@SKILL_2120_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 500

; #region == 총력전 ==
@총력전_스킬_2120_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
RETURN 3

; alt.opt.1 2단 히트로 변경, 명중 하면 지속 대미지
@총력전_스킬_2120_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 1범위 적에게 대미지 (공격력 500％), 2턴간 확정 지속 대미지 부여 (공격력 27％). 3턴마다 2턴 동안 공격력 증가 (+30％)
; RESULTS = 자신의 공격력 63％ 증가 추가로 적에게 공격력 501％ 대미지 2턴간 공격력 27％ 화상부여

; 카운터 추가
@총력전_스킬_2120_초기(시전자)
#DIM 시전자
CALL 총력전_카운터_추가(시전자, 시전자, 총력전_방식_턴, 총력전_방식_지속, 1, 2120)

@총력전_스킬_2120_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 주목표
#DIM DYNAMIC 범위목표, 총력전_캐릭터수
#DIM DYNAMIC 대미지
#DIM DYNAMIC 지속대미지
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「내가, 백화요란의 돌격대장이다!」
sTmp:1 = 「하! 버텨보라고!」
sTmp:2 = 「어때, 이게 백화요란의 사격술이라고!」
CALL SKILL_2120_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 140, RESULTS, sTmp)
; 목표 처리
CALL 총력전_시퀀스_목표선택(주목표, 총력전_선택_적군)
; 목표 없으면 스킬 해재
IF 주목표 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 범위로 변경, 적팀 1 이상이 되면 수정 필요
CALL 총력전_목표_범위(범위목표, 주목표, 1, 1)
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 500
대미지 = DIVIDE_ROUND(대미지, 100)
지속대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 27
지속대미지 = DIVIDE_ROUND(지속대미지, 100)
FOR iTmp, 0, 총력전_캐릭터수
	SIF 범위목표:iTmp < 0
		BREAK
	CALL 총력전_처리_피격(대미지, 범위목표:iTmp, 시전자)
	CALL 총력전_상태효과_추가_텍스트(범위목표:iTmp, 시전자, 상효_종류_대미지, 지속대미지, 0, 총력전_방식_턴, 총력전_방식_턴, 2, 2120, 0)
NEXT

; #DIM 대미지
; #DIM 공격력증가량
; 공격력증가량 = 63
; 렌게화상대미지 = (총력전_공격력:LOCAL * 27 / 100)
; 대미지 = (총력전_공격력:LOCAL * 501 / 100) * (100 - 총력전_보스_방어력) / 100
; CALL 버프처리(1,,,,,공격력증가량,,,,,,,,2120,,LOCAL)
; CALL 공격판정처리(대미지,LOCAL)
; CALL 디버프부여처리("화상",2120,2,LOCAL,렌게화상대미지)


; #region == 카운터 ==
; 열정 / 돌격대장으로서의 본분
@총력전_스킬_2120_0_카운터_증가(캐릭터, 대상, 카운터번호, 증가양)
#DIM 캐릭터
#DIM 대상
#DIM 카운터번호
#DIM 증가양

총력전_카운터_증가양:캐릭터:카운터번호 += 증가양
IF 총력전_카운터_증가양:캐릭터:카운터번호 >= 3
	총력전_카운터_증가양:캐릭터:카운터번호 = 0
	CALL 총력전_상태효과_추가_텍스트(캐릭터, 캐릭터, 상효_종류_공격력백분율, 30, 1, 총력전_방식_지속, 총력전_방식_턴, 2, 2120, 1)
ENDIF