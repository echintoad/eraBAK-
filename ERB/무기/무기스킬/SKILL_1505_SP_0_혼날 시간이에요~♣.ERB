;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1505_SP_0_EXIST
RETURN 1

@SKILL_1505_SP_0_NAME
RESULTS = 혼날 시간이에요~♣

;레벨은 1-5까지
@SKILL_1505_SP_0_LEVEL
RETURN 2

;대상 선택
@SKILL_1505_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_1505_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
 
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1505_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1505_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
LOCAL:1 = RAND(3, 6)
FOR LOCAL, 0, LOCAL:1
	CALL DECREASE_SOLDIER(대상세력, 대상부대, ABL:발동자:무투*5, 1)
NEXT

@SKILL_1505_SP_0_EXPLANATION
RESULTS = 적 부대에 3~6 회의 데미지를 준다


@SKILL_1505_SP_0_CANT_TELL

; 아이가 배울수 없도록 한다
@SKILL_1505_SP_0_CANT_INHERIT


@SKILL_1505_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 500

; #region == 총력전 ==
@총력전_스킬_1505_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 5

@총력전_스킬_1505_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
; 820%
RESULTS = 1턴간 자신의 치명대미지 30％ 증가 추가로 모든 적에게 공격력 410％x2 대미지

@총력전_스킬_1505_발동(시전자)
#DIM 시전자

#DIM 목표, 총력전_캐릭터수
#DIM 대미지
#DIM 치댐증가 = 30
#DIM iTmp, 2
#DIMS sTmp
; 발동 텍스트
CALL SKILL_1505_SP_0_NAME
sTmp = %총력전_이름:시전자% 스킬 시전:『%RESULTS%』
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
iTmp = 총력전_ID:시전자
IF iTmp >= 0 && NO:iTmp == 4
	; 노노미일 경우
	; 쿠로코 옵션 추가?
	SELECTCASE RAND:3
		CASE 0
			sTmp = 「응응! 준비 완료~」
		CASE 1
			; 이름 바꿀거를 대비해서?
			sTmp = 「%ANAME(iTmp)%~ 갑니다~」
		CASEELSE
			sTmp = 「전탄 발사~」
	ENDSELECT
	CALL 총력전_UI_텍스트_추가(sTmp)
ENDIF
; 버프 추가
CALL 총력전_상태효과_추가_텍스트(시전자, 시전자, 상효_종류_치명타대미지, 치댐증가, 1, 총력전_방식_지속, 총력전_방식_턴, 1, 1505)
; 버프 텍스트
; sTmp = %조사처리(총력전_이름:시전자, "의")% 치명타 대미지 증가(30％)
; CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
; 목표 처리
; IF 총력전_팀:시전자 == 0
; 	iTmp = 1
; ELSE
; 	iTmp = 0
; ENDIF
CALL 총력전_목표_전체(목표, 1)
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 410
대미지 = DIVIDE_ROUND(대미지, 100)
FOR iTmp:1, 0, 2
	VARSIZE 목표
	FOR iTmp, 0, RESULT
		SIF 목표:iTmp < 0
			BREAK ; CONTINUE?
		CALL 총력전_처리_피격(대미지, 목표:iTmp, 시전자)
	NEXT
NEXT