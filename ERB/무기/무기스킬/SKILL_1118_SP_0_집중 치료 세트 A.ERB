;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1118_SP_0_EXIST
RETURN 1

@SKILL_1118_SP_0_NAME
RESULTS = 집중 치료 세트 A

;레벨은 1-5까지
@SKILL_1118_SP_0_LEVEL
RETURN 5

;대상 선택
@SKILL_1118_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_1118_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1118_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1118_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "상태가 나쁘신 분부터 와 주세요."
CALL INCREASE_SOLDIER(발동세력, 발동부대, (BATTLE_지략파워:발동측:발동번호 / 35), 1)

@SKILL_1118_SP_0_EXPLANATION
RESULTS = 자신 부대 지략합에 비례해 부대의 병력을 회복시킨다.

@SKILL_1118_SP_0_CANT_TELL

; 아이가 배울수 없도록 한다
@SKILL_1118_SP_0_CANT_INHERIT


@SKILL_1118_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 250

; #region == 총력전 ==
@총력전_스킬_1118_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 2

@총력전_스킬_1118_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 대상을 이동후 치유력 228％ 회복

@총력전_스킬_1118_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 대상
#DIM DYNAMIC 치유량
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「영차, 보급품이 도착했어요!」
sTmp:1 = 「여러분을 위한 보급품이에요!」
sTmp:2 = 「상태가 나쁘신 분부터 와 주세요.」
CALL SKILL_1118_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 66, RESULTS, sTmp)
; 이동 + 치유 대상 처리
sTmp = 치유 + 이동 대상 선택
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
sTmp = 치유만 원하면 1 캐릭터 선택후 선택 종료
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
CALL 총력전_시퀀스_이동(0)
대상 = RESULT
; 목표 없으면 스킬 해재
; 대상 0 이면 이동 하지 않았다 가정
IF 대상 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 치유
치유량 = GETVAR_RAIDSTAT_SUM(시전자, "치유력") * 228
치유량 = DIVIDE_ROUND(치유량, 100)
CALL 총력전_처리_치유(치유량, 대상, 시전자)


; 대상수 = 1
; 치유량 = 총력전_치유력:LOCAL * 228 /100
; CALL 버프처리(1,,,치유량,,,,,,,,,대상수,1118)
