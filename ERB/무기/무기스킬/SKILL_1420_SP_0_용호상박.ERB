;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1420_SP_0_EXIST
RETURN 1

@SKILL_1420_SP_0_NAME
RESULTS = 용호상박

;레벨은 1-5까지
@SKILL_1420_SP_0_LEVEL
RETURN 3

;대상 선택
@SKILL_1420_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_1420_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1


;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1420_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1420_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측

PRINTFORML 적 부대의 무투가 감소했다!

CALL DECREASE_SOLDIER(대상세력, 대상부대, ABL:발동자:무투*5, 1)
TIMES BATTLE_무투파워:대상측:발동번호, 0.75
TIMES BATTLE_방위파워:대상측:발동번호, 0.75

@SKILL_1420_SP_0_EXPLANATION
RESULTS = 적 부대에 데미지를 주고 무투와 방위를 감소시킨다.

@SKILL_1420_SP_0_CANT_TELL


@SKILL_1420_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 400

; #region == 총력전 ==
@총력전_스킬_1420_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 3

; 반 테스트, 반 스1 반영, 디버프 수 추가 대미지
@총력전_스킬_1420_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
; 3턴 으로?
RESULTS = 적에게 공격력 디버프수(최대8) x 50％ + 500％ 대미지, 명중시 4턴간 적의 공격력 (-20％), 명중률 (-20％), 회피률 (-20％) 감소
; RESULTS = 2턴간 자신의 공격력, 회피율, 명중률 35％ 증가 적의 공격력 30％ 감소 추가로 적에게 공격력 593％ 대미지 

@총력전_스킬_1420_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 목표
#DIM DYNAMIC 대미지
#DIM CONST 공격감소 = -20
#DIM CONST 명중감소 = -20
#DIM CONST 회피감소 = -20
#DIM CONST 지속시간 = 4
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「후우… 미래를 위해서.」
sTmp:1 = 「작별이다.」
sTmp:2 = 「비가 오겠군.」
CALL SKILL_1420_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 118, RESULTS, sTmp)
; 목표 처리
CALL 총력전_시퀀스_목표선택(목표, 총력전_선택_적군)
; 목표 없으면 스킬 해재
IF 목표 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 대미지
대미지 = MIN(총력전_상태효과_수_긍부정(목표, 0), 8) * 50 + 500
[IF_DEBUG]
sTmp = 디버프수 = {MIN(총력전_상태효과_수_긍부정(목표, 0), 8)}
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_파노랑)
[ENDIF]
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 대미지
대미지 = DIVIDE_ROUND(대미지, 100)
CALL 총력전_처리_피격(대미지, 목표, 시전자)
; 명중시 디버프
IF RESULT == 1
	CALL 총력전_상태효과_추가(목표, 시전자, 상효_종류_공격력백분율, 공격감소, 0, 총력전_방식_지속, 총력전_방식_턴, 지속시간, 1420, 0)
	CALL 총력전_상태효과_추가(목표, 시전자, 상효_종류_명중률백분율, 명중감소, 0, 총력전_방식_지속, 총력전_방식_턴, 지속시간, 1420, 1)
	CALL 총력전_상태효과_추가(목표, 시전자, 상효_종류_회피율백분율, 회피감소, 0, 총력전_방식_지속, 총력전_방식_턴, 지속시간, 1420, 2)
	; 텍스트 추가
	sTmp = {지속시간}턴간 %조사처리(총력전_이름:목표, "의")% 공격력 ({공격감소}％), 명줄률 ({명중감소}％), 회피율 ({회피감소}％) 감소.
	CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
	; UI 업데이트
	CALL 총력전_UI_Main(0)
	WAITANYKEY
ENDIF
