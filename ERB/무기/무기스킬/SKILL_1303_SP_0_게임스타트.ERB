;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1303_SP_0_EXIST
RETURN 1

@SKILL_1303_SP_0_NAME
RESULTS = 게임, 스타트!

;레벨은 1-5까지
@SKILL_1303_SP_0_LEVEL
RETURN 3

;대상 선택
@SKILL_1303_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_1303_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1303_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1303_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "레트로여...영원하라!"
CALL DECREASE_SOLDIER(대상세력, 대상부대, RAND(ABL:발동자:무투*5 , ABL:발동자:무투*50), 1)

@SKILL_1303_SP_0_EXPLANATION
RESULTS = 적에게 데미지를 준다.


@SKILL_1303_SP_0_CANT_TELL

; 아이가 배울수 없도록 한다
@SKILL_1303_SP_0_CANT_INHERIT


@SKILL_1303_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 550

; #region == 총력전 ==
@총력전_스킬_1303_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 4

@총력전_스킬_1303_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 1 범위 적에게 공격력 594％ 대미지. 기본 공격 3회 또는 스킬 시전시 2턴간 치명타 대미지 69％ 증가.
; RESULTS = 1턴간 자신의 치명대미지 69％ 증가 추가로 적에게 공격력 594％ 대미지

; 총력전 시작시 시전
@총력전_스킬_1303_초기(시전자)
#DIM 시전자
; 기본 공격 카운터 추가
CALL 총력전_카운터_추가(시전자, 시전자, 총력전_방식_공격, 총력전_방식_지속, 1, 1303)


@총력전_스킬_1303_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 주목표
#DIM DYNAMIC 범위목표, 총력전_캐릭터수
#DIM DYNAMIC 대미지
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「조준은… 끝났습니다.」
sTmp:1 = 「레트로여… 영원하라!」
sTmp:2 = 「각오하세요!」
CALL SKILL_1303_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 35, RESULTS, sTmp)
; 목표 처리
CALL 총력전_시퀀스_목표선택(주목표, 총력전_선택_적군)
; 목표 없으면 스킬 해재
IF 주목표 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 범위로 변경, 적팀 1 이상이 되면 수정 필요
CALL 총력전_목표_범위(범위목표, 주목표, 1, 1)
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 594
대미지 = DIVIDE_ROUND(대미지, 100)
FOR iTmp, 0, 총력전_캐릭터수
	SIF 범위목표:iTmp < 0
		BREAK
	CALL 총력전_처리_피격(대미지, 범위목표:iTmp, 시전자)
NEXT
CALL _총력전_스킬_1303_버프추가(시전자)


; #DIM 대미지
; #DIM 치명대미지증가량
; 치명대미지증가량 = 69
; 대미지 = (총력전_공격력:LOCAL * 594 / 100) * (100 - 총력전_보스_방어력) / 100
; CALL 버프처리(1,,,,,,,치명대미지증가량,,,,,,1303,,LOCAL)
; CALL 공격판정처리(대미지,LOCAL)

; @총력전_스킬_1303_버프지속
; RETURN 1


; #region == 카운터 ==
@총력전_스킬_1303_0_카운터_증가(캐릭터, 대상, 카운터번호, 증가양)
#DIM 캐릭터
#DIM 대상
#DIM 카운터번호
#DIM 증가양

#DIM CONST 치명타대미지증가량 = 69

총력전_카운터_증가양:캐릭터:카운터번호 += 증가양
; 2번 공격후 치명타 대미지 증가
IF 총력전_카운터_증가양:캐릭터:카운터번호 >= 3
	CALL _총력전_스킬_1303_버프추가(캐릭터)
	; 카운터 초기화
	총력전_카운터_증가양:캐릭터:카운터번호 = 0
ENDIF


; #region == 개인 함수? ==
; 에라엔 private function 이 없는걸로 알지만...
; 버프 추가용
@_총력전_스킬_1303_버프추가(캐릭터)
#DIM 캐릭터

#DIM CONST 치명타대미지증가량 = 69
#DIMS DYNAMIC sTmp

CALL 총력전_상태효과_추가_텍스트(캐릭터, 캐릭터, 상효_종류_치명타대미지, 치명타대미지증가량, 1, 총력전_방식_지속, 총력전_방식_턴, 2, 1303)
; 텍스트 추가
; sTmp = %조사처리(총력전_이름:캐릭터, "의")% 치명타 대미지 ({치명타대미지증가량}％) 증가!
; CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
; ; UI 업데이트
; CALL 총력전_UI_Main(0)
; WAITANYKEY