;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1902_BASE_0_EXIST
RETURN 1

@SKILL_1902_BASE_0_NAME
RESULTS = 신념과 긍지

;레벨은 1-5까지
@SKILL_1902_BASE_0_LEVEL
RETURN 4

;대상 선택
@SKILL_1902_BASE_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_1902_BASE_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1902_BASE_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1902_BASE_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "전장에 구호의 손길을!"
CALL DECREASE_SOLDIER(대상세력, 대상부대, RAND(ABL:발동자:방위*7 , ABL:발동자:방위*14), 1)
TIMES BATTLE_DEF:대상측, 0.85


@SKILL_1902_BASE_0_EXPLANATION
RESULTS = 적에게 데미지를 주고 방어력을 감소시킨다.

@SKILL_1902_BASE_0_CANT_TELL


@SKILL_1902_BASE_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 350

; #region == 총력전 ==
@총력전_스킬_1902_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 4

; alt.opt.1 회피 감소 + 방어력감소 가능, 트리니티 버프 컨셉 시도해보는 동안은 명중률 버프 유지
; alt.opt.2 지속 치유 계산 = 치유력 30% + 방어력 30%?
; alt.opt.3 적 지정 -> 이동 위치 선택 -> 이동 + 대미지 -> 디버프 -> 버프 -> 지속 치유
; 이제 명중률 명중률백분율 이 따로 있어서... 일단은 명중률백분율 로 유지
@총력전_스킬_1902_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 적에게 공격력 196％ 대미지 이후 2턴간 적과 주변 1범위의 적군 방어력 26％ 감소, 아군 전원의 명중률 26％ 증가, 자신에게 2턴간 치유력 40％ 지속 치유
; RESULTS = 적에게 공격력 196％ 대미지 이후 2턴간 적의 방어력 26％ 감소 아군 전원의 명중률 26％ 증가

@총력전_스킬_1902_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 주목표
#DIM DYNAMIC 범위목표, 총력전_캐릭터수
#DIM DYNAMIC 버프대상, 총력전_캐릭터수
#DIM DYNAMIC 대미지
#DIM DYNAMIC 치유량
#DIM CONST 방깎수치 = -26
#DIM CONST 명중률증가량 = 26
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「가겠습니다!」
sTmp:1 = 「전장에 구호의 손길을!」
sTmp:2 = 「신념과 긍지를 가슴에!」
CALL SKILL_1902_BASE_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 65, RESULTS, sTmp)
; 목표 처리
CALL 총력전_시퀀스_목표선택(주목표, 총력전_선택_적군)
; 목표 없으면 스킬 해재
IF 주목표 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 196
대미지 = DIVIDE_ROUND(대미지, 100)
CALL 총력전_처리_피격(대미지, 주목표, 시전자)
; 범위로 변경, 적팀 1 이상이 되면 수정 필요
CALL 총력전_목표_범위(범위목표, 주목표, 1)
; 디버프
FOR iTmp, 0, 총력전_캐릭터수
	SIF 범위목표:iTmp < 0
		BREAK
	CALL 총력전_상태효과_추가_텍스트(범위목표:iTmp, 시전자, 상효_종류_방어력백분율, 방깎수치, 0, 총력전_방식_지속, 총력전_방식_턴, 2, 1902, 0)
	; 텍스트 추가
	; sTmp = %조사처리(총력전_이름:(범위목표:iTmp), "의")% 방어력 ({방깎수치}％) 감소!
	; CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
	; ; UI 업데이트
	; CALL 총력전_UI_Main(0)
	; WAITANYKEY
NEXT
; 버프 목표 처리
CALL 총력전_목표_전체(버프대상, 0)
; 버프
FOR iTmp, 0, 총력전_캐릭터수
	SIF 버프대상:iTmp < 0
		BREAK
	CALL 총력전_상태효과_추가_텍스트(버프대상:iTmp, 시전자, 상효_종류_명중률백분율, 명중률증가량, 1, 총력전_방식_지속, 총력전_방식_턴, 2, 1902, 1)
	; 텍스트 추가
	; sTmp = 2턴간 %조사처리(총력전_이름:(버프대상:iTmp), "의")% 명중률 ({명중률증가량}％) 증가!
	; CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
	; ; UI 업데이트
	; CALL 총력전_UI_Main(0)
	; WAITANYKEY
NEXT
; 지속치유
치유량 = GETVAR_RAIDSTAT_SUM(시전자, "치유력") * 80
치유량 = DIVIDE_ROUND(치유량, 100)
CALL 총력전_상태효과_추가_텍스트(시전자, 시전자, 상효_종류_치유량, 치유량, 1, 총력전_방식_턴, 총력전_방식_턴, 2, 1902, 2)
; 텍스트 추가
; sTmp = 2턴간 %조사처리(총력전_이름:시전자, "가")% 지속 치유 ({치유량})!
; CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
; ; UI 업데이트
; CALL 총력전_UI_Main(0)
; WAITANYKEY


; 방깎수치 = 26
; 명중률증가량 = 26
; 대미지 = (총력전_공격력:LOCAL * 196 / 100) * (100 - 총력전_보스_방어력) / 100
; CALL 공격판정처리(대미지,LOCAL)
; CALL 디버프부여처리("방깎",1902,2,LOCAL,방깎수치)
; CALL 버프처리(1,,,,,,,,명중률증가량,,,,5,1902)

; @총력전_스킬_1902_버프지속
; RETURN 2