;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1401_SP_0_EXIST
RETURN 1

@SKILL_1401_SP_0_NAME
RESULTS = 강철의 비

;레벨은 1-5까지
@SKILL_1401_SP_0_LEVEL
RETURN 4

;대상 선택
@SKILL_1401_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
RETURN 1

;발동 판정
@SKILL_1401_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1401_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1401_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "크흐흣~! 전탄발사앗~!!"
LOCAL:1 = RAND(4, 15)
FOR LOCAL, 0, LOCAL:1
	CALL DECREASE_SOLDIER(대상세력, 대상부대, ABL:발동자:무투*3, 1)
NEXT

@SKILL_1401_SP_0_EXPLANATION
RESULTS = 적 부대에 연속 데미지를 준다.


@SKILL_1401_SP_0_CANT_TELL

; 아이가 배울수 없도록 한다
@SKILL_1401_SP_0_CANT_INHERIT


@SKILL_1401_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 600

; #region == 총력전 ==
@총력전_스킬_1401_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
RETURN 4

@총력전_스킬_1401_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 모든 적에게 공격력 245％x2 대미지 추가로 2턴간 공격력 100％ 화상부여

@총력전_스킬_1401_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 목표, 총력전_캐릭터수
#DIM DYNAMIC 대미지
#DIM DYNAMIC 공격명중
#DIM DYNAMIC 지속대미지
#DIM DYNAMIC iTmp, 2
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「크흐흣~! 전탄발사앗~!!」
sTmp:1 = 「하아, 하아… 더 이상은 못 참아!」
sTmp:2 = 「하아, 하아… 아름다워…」
CALL SKILL_1401_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 109, RESULTS, sTmp)
; 목표 처리
CALL 총력전_목표_전체(목표, 1)
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 245
대미지 = DIVIDE_ROUND(대미지, 100)
지속대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 100
지속대미지 = DIVIDE_ROUND(지속대미지, 100)
FOR iTmp, 0, 총력전_캐릭터수
	SIF 목표:iTmp < 0
		BREAK
	공격명중 = 0
	FOR iTmp:1, 0, 2
		CALL 총력전_처리_피격(대미지, 목표:iTmp, 시전자)
		공격명중 = 1
	NEXT
	; 명중시? 지속 대미지?
	IF 공격명중 == 1
		CALL 총력전_상태효과_추가_텍스트(목표:iTmp, 시전자, 상효_종류_대미지, 지속대미지, 0, 총력전_방식_턴, 총력전_방식_턴, 2, 1401)
	ENDIF
NEXT





; 모에화상대미지 = (총력전_공격력:LOCAL * 100 / 100)
; 대미지 = (총력전_공격력:LOCAL * 489 / 100) * (100 - 총력전_보스_방어력) / 100
; CALL 공격판정처리(대미지,LOCAL)
; CALL 디버프부여처리("화상",1401,2,LOCAL,모에화상대미지)