;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_2006_SP_0_EXIST
RETURN 1

@SKILL_2006_SP_0_NAME
RESULTS = 관측 지원

;레벨은 1-5까지
@SKILL_2006_SP_0_LEVEL
RETURN 5

;대상 선택
@SKILL_2006_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
SIF BATTLE_COMMANDER_NUM:대상측 == 0
	RETURN 0
COMBAT_SKILL_TARGET = RAND:(BATTLE_COMMANDER_NUM:대상측)
RETURN !BATTLE_SUMMONED_CHARA:대상측:COMBAT_SKILL_TARGET


;발동 판정
@SKILL_2006_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1


;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_2006_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_2006_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "제 눈을 벗어날 순 없어요!"
BATTLE_RATE_ATC:대상측 += 5
CAPTURE_RATE:COMBAT_SKILL_TARGET += 400


@SKILL_2006_SP_0_EXPLANATION
RESULTS = 적 부대의 받는 데미지를 증가시키고 포박률을 올린다.

@SKILL_2006_SP_0_CANT_TELL

; 아이가 배울수 없도록 한다
@SKILL_2006_SP_0_CANT_INHERIT


@SKILL_2006_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 400

; #region == 총력전 ==
@총력전_스킬_2006_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 3

@총력전_스킬_2006_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
; 테스트 하는 김에, 나중에 시전자 주위 / 지정 범위로 변경
RESULTS = 모든 아군에게 (시전가 제외) 3턴간 명중률 증가 (시전자에 명중률 60％)
; RESULTS = 최대 3인에게 3턴간 명중률 47％ 증가

@총력전_스킬_2006_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 버프대상, 총력전_캐릭터수
#DIM DYNAMIC 명중증가
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「관측 준비 완료!」
sTmp:1 = 「제 눈을 벗어날 순 없어요!」
sTmp:2 = 「후후~ 거기 있었네요」
CALL SKILL_2006_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 99, RESULTS, sTmp)
; 목표 처리
CALL 총력전_목표_전체(버프대상, 0)
; 명중률 처리
명중증가 = GETVAR_RAIDSTAT_SUM(시전자, "명중률") * 60
명중증가 = DIVIDE_ROUND(명중증가, 100)
FOR iTmp, 0, 총력전_캐릭터수
	SIF 버프대상:iTmp < 0
		BREAK
	SIF 버프대상:iTmp == 시전자
		CONTINUE
	CALL 총력전_상태효과_추가_텍스트(버프대상:iTmp, 시전자, 상효_종류_명중률, 명중증가, 0, 총력전_방식_지속, 총력전_방식_턴, 3, 2006)
NEXT
