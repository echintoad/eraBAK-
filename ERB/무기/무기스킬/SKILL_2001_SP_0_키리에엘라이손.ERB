;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_2001_SP_0_EXIST
RETURN 1

@SKILL_2001_SP_0_NAME
RESULTS = Kyrie Eleison

;레벨은 1-5까지
@SKILL_2001_SP_0_LEVEL
RETURN 5

;대상 선택
@SKILL_2001_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상

RETURN 1

;발동 판정
@SKILL_2001_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_2001_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_2001_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대

대상측 = !발동측
; PRINTFORML "너희들을 위해, 기도할게."
CALL DECREASE_SOLDIER(대상세력, 대상부대, RAND(ABL:발동자:무투*30 , ABL:발동자:무투*50), 1)


@SKILL_2001_SP_0_EXPLANATION
RESULTS = 자신 무투에 비례해 데미지를 준다.

@SKILL_2001_SP_0_CANT_TELL


@SKILL_2001_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 200

; #region == 총력전 ==
@총력전_스킬_2001_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 6

@총력전_스킬_2001_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 적에게 공격력 1540％ 확정 치명 대미지 대상의 현재체력에 비례하여 최대 2배까지 대미지 상승

@총력전_스킬_2001_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 목표
#DIM DYNAMIC 대미지
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 3

; 발동 텍스트
sTmp:0 = 「너희를 위해… 기도할게.」
sTmp:1 = 「너희는 지나갈 수 없어.」
sTmp:2 = 「여기는 내가 지킬 테니까.」
CALL SKILL_2001_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 51, RESULTS, sTmp)
; 목표 처리
CALL 총력전_시퀀스_목표선택(목표, 총력전_선택_적군)
; 목표 없으면 스킬 해재
IF 목표 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 대미지
대미지 = GETVAR_RAIDSTAT_SUM(시전자, "공격력") * 1540 * REMAP_CLAMP(총력전_체력:목표, 0, GETVAR_RAIDSTAT_SUM(목표, "최대체력"), 100, 200)
대미지 = DIVIDE_ROUND(대미지, 10000)
; 계산 확인
[IF_DEBUG]
iTmp = GETVAR_RAIDSTAT_SUM(시전자, "공격력")
sTmp = 공격력:{iTmp} * 1540
iTmp = iTmp * 1540
sTmp += @" = {iTmp}"
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_파노랑)
sTmp = 체력 추가 대미지 비율:{REMAP_CLAMP(총력전_체력:목표, 0, GETVAR_RAIDSTAT_SUM(목표, "최대체력"), 100, 200)}％
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_파노랑)
iTmp = iTmp * REMAP_CLAMP(총력전_체력:목표, 0, GETVAR_RAIDSTAT_SUM(목표, "최대체력"), 100, 200)
sTmp = * {REMAP_CLAMP(총력전_체력:목표, 0, GETVAR_RAIDSTAT_SUM(목표, "최대체력"), 100, 200)} = {iTmp}
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_파노랑)
iTmp = DIVIDE_ROUND(iTmp, 10000)
sTmp = /10000 = {iTmp}
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_파노랑)
[ENDIF]
; 시전자, 1[=회피 가능], 2[=확정 크리])
CALL 총력전_처리_피격(대미지, 목표, 시전자, 1, 2)

; #DIM 체력비
; 체력비 = 총력전_보스_현재체력 * 100 / 총력전_보스_최대체력
; 대미지 = (총력전_공격력:LOCAL * 1540 / 100) * (100 - 총력전_보스_방어력) / 100 * 총력전_보스_현재체력 * 100 / 총력전_보스_최대체력 / 100
; 대미지 = 대미지 + (총력전_공격력:LOCAL * 1540 / 100) * (100 - 총력전_보스_방어력) / 100
; CALL 공격판정처리(대미지,LOCAL,1)