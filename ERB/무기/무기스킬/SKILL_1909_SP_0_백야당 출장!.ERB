;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_1909_SP_0_EXIST
RETURN 1

@SKILL_1909_SP_0_NAME
RESULTS = 백야당 출장!

;레벨은 1-5까지
@SKILL_1909_SP_0_LEVEL
RETURN 4

;대상 선택
@SKILL_1909_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
;부대 대상
SIF BATTLE_COMMANDER_NUM:발동측 == 0
	RETURN 0
COMBAT_SKILL_TARGET = RAND:(BATTLE_COMMANDER_NUM:발동측)
RETURN 1

;발동 판정
@SKILL_1909_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN 1

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_1909_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_1909_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
; PRINTFORML "가, 가게의 물건인데!"
BATTLE_RATE_GRD:발동측 += 15
PRINTFORML %ANAME(BATTLE_COMMANDER:발동측:COMBAT_SKILL_TARGET)%의 스킬이 발동하기 쉬워졌다!
BATTLE_SKILL_RATE:발동측:COMBAT_SKILL_TARGET += 25


@SKILL_1909_SP_0_EXPLANATION
RESULTS = 자신 부대의 받는 피해를 감소시키고 아군 한 명의 스킬 발동을 발동하기 쉽게 한다.

@SKILL_1909_SP_0_CANT_TELL


@SKILL_1909_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 400

; #region == 총력전 ==
@총력전_스킬_1909_코스트(시전자)
#DIM 시전자 ; 코스트 당 대미지 스킬 카운터 위해서
; #DIM 가변
RETURN 3

@총력전_스킬_1909_설명(시전자)
#DIM 시전자 ; 가변스킬 설명 위해서
RESULTS = 3턴간 시전자 최대 체력 46％의 엄폐물 생성. 엄폐물 2범위 주변에 아군 명중률 21％ 증가.
; RESULTS = 최대 세명의 아군에게 3턴간 명중률 21％ 증가 추가로 최대 체력 46％의 엄폐물 생성

@총력전_스킬_1909_발동(시전자)
#DIM 시전자

#DIM DYNAMIC 엄폐물위치
#DIM DYNAMIC 소비방식, 2
#DIM DYNAMIC 소비양, 2
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp, 2

; 발동 텍스트
sTmp:0 = 「백야당은 어디든 출장 판매합니다! 앗…! 다기가……!」
sTmp:1 = 「가, 가게의 물건인데!」
CALL SKILL_1909_SP_0_NAME
CALL 총력전_스킬_공통_발동텍스트(시전자, 78, RESULTS, sTmp)
; 소환 위치 처리
sTmp = 엄폐물 소환 위치 선택
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
CALL 총력전_시퀀스_목표선택(엄폐물위치, 총력전_선택_아군지역_엄폐물)
; 엄폐물 번호가 -1 이면 스킬 해재
IF 엄폐물위치 < 0
	CALL 총력전_스킬_공통_스킬취소(시전자)
	RETURN
ENDIF
; 엄폐물 중복 확인
iTmp = 총력전_엄폐물_검색_스킬번호(1909)
; 있으면 엄폐물 제거
IF iTmp >= 0
	; CALL 총력전_엄폐물_제거_텍스트(iTmp) ; 옵션 2
	CALL 총력전_해제_엄폐물(iTmp)
ENDIF
; 총력전 번호 -> 위치로 변경
엄폐물위치 = 총력전_위치:엄폐물위치
; 엄폐물 소환, 체력 0 또는 3 턴 이후 소멸
소비방식:0 = 총력전_방식_피격양
소비양:0 = 총력전_최대체력_최초:시전자 * 48
소비양:0 = DIVIDE_ROUND(소비양:0, 100)
소비방식:1 = 총력전_방식_턴
소비양:1 = 3
CALL 총력전_엄폐물_추가(엄폐물위치, 시전자, 소비방식, 소비양, 1909)


; #region == 엄폐물 ==
; 턴 시작시 스킬
@총력전_엄폐물_1909_0_스킬(엄폐물번호)
#DIM 엄폐물번호

#DIM DYNAMIC 버프대상, 총력전_캐릭터수
#DIM CONST 명중증가 = 21
#DIM DYNAMIC iTmp
#DIMS DYNAMIC sTmp
; 위치 대상 선택
iTmp = 총력전_엄폐물_위치:엄폐물번호
iTmp = FINDELEMENT(총력전_위치, iTmp)
; 범위 대상, 적팀 1 이상이 되면 수정 필요
CALL 총력전_목표_범위(버프대상, iTmp, 0, 2)
; 버프
FOR iTmp, 0, 총력전_캐릭터수
	SIF 버프대상:iTmp < 0
		BREAK
	CALL 총력전_상태효과_추가(버프대상:iTmp, -1, 상효_종류_명중률백분율, 명중증가, 1, 총력전_방식_지속, 총력전_방식_턴, 1, 1909)
	; CALL 총력전_상태효과_추가_텍스트(버프대상:iTmp, -1, 상효_종류_명중률백분율, 명중증가, 1, 총력전_방식_지속, 총력전_방식_턴, 1, 1909)
NEXT
CALL SKILL_1909_SP_0_NAME
sTmp = 『%RESULTS%』엄폐물 주변 명중률 증가 ({명중증가})
CALL 총력전_UI_텍스트_추가(sTmp, 칼라_노랑)
